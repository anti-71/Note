---
名称: 线程安全问题
章: 03 Java
节: "[[第一章 多线程]]"
tags:
  - 知识点
index: 5
---
##### 问题描述

多个线程同时操作同一个共享资源时，可能出现业务安全问题

##### 案例：取钱

小明和小红有一个共同账户（余额 10 万），两人同时取钱 10 万

```java
public class Account {
    private String cardId;
    private double money;

    public void drawMoney(double money) {
        String name = Thread.currentThread().getName();
        if (this.money >= money) {
            System.out.println(name + "取钱成功，吐出" + money + "元");
            this.money -= money;
            System.out.println(name + "取钱后余额：" + this.money + "元");
        } else {
            System.out.println(name + "取钱失败，余额不足");
        }
    }
}
```

不加同步时，可能出现两人同时判定余额足够，都取走 10 万的情况
