---
名称: 外部表操作
章: 02 大数据
节: "[[第五章 Apache Hive 使用语法与概念原理]]"
tags:
  - 知识点
index: 4
---
##### 外部表的创建

外部表，创建表被 EXTERNAL 关键字修饰，从概念是被认为并非 Hive 拥有的表，只是 **临时关联数据** 去使用

创建外部表也很简单，基于外部表的特性，可以总结出：外部表和数据是相互独立的，即：

- 可以先有 **表**，然后把数据移动到表指定的 LOCATION 中
- 也可以先有 **数据**，然后创建表通过 LOCATION 指向数据

1. 在 Linux 上创建新文件，test_external.txt，并填入如下内容：

```plaintext
1	itheima
2	itcast
3	hadoop
```

数据列用 “\t” 分隔

2. 演示先 **创建外部表**，然后 **移动数据** 到 LOCATION 目录

- 首先检查：`hadoop fs -ls /tmp`，确认不存在 ‘/tmp/test_ext1’ 目录
- 创建外部表：

```sql
create external table test_ext1(id int, name string) row format delimited fields terminated by '\t' location '/tmp/test_ext1';
```

- 可以看到，目录 /tmp/test_ext1 被创建
- `select * from test_ext1`，空结果，无数据
- 上传数据：`hadoop fs -put test_external.txt /tmp/test_ext1/`
- `select * from test_ext1`，即可看到数据结果

3. 演示先 **存在数据**，后 **创建外部表**

- `hadoop fs -mkdir /tmp/test_ext2`
- `hadoop fs -put test_external.txt /tmp/test_ext2/`
- 创建外部表：

```sql
create external table test_ext2(id int, name string) row format delimited fields terminated by '\t' location '/tmp/test_ext2';
```

- `select * from test_ext2;`

##### 内外部表转换

Hive 可以很简单的通过 SQL 语句转换内外部表

- 查看表类型：`desc formatted stu;`

- 内部表转外部表

```sql
alter table stu set tblproperties('EXTERNAL'='TRUE');
```

- 外部表转内部表

```sql
alter table stu set tblproperties('EXTERNAL'='FALSE');
```

通过 `stu set tblproperties` 来修改属性

> [!warning] 
> ('EXTERNAL'='FALSE') 或 ('EXTERNAL'='TRUE') 为固定写法，区分大小写！!