# 第六章 定积分的应用

### 一、几何应用

#### 1.平面图形的面积

（1）若平面域$ D $ 由曲线$ y = f(x) $，$ y = g(x) $（$ f(x) \geq g(x) $），$ x = a $，$ x = b $（$ a < b $所围成，则平面域$ D $的面积为
$$
 S = \int_{a}^{b} [f(x) - g(x)] dx
$$
（2）若平面域$ D $由曲线$ r = r(\theta) $，$ \theta = \alpha $，$ \theta = \beta $（$ \alpha < \beta $）所围成，则其面积为
$$
S = \frac{1}{2} \int_{\alpha}^{\beta} r^{2}(\theta) d\theta
$$
更一般地，
$$
S=\iint_D1\ d\sigma
$$

#### 2.旋转体体积

若区域$ D $由曲线$ y = f(x) $（$ f(x) \geq 0 $）和直线$ x = a $，$ x = b $（$ 0 \leq a < b $）及$ x $轴所围成，则：

（1）区域$ D $绕$ x $轴旋转一周所得到的旋转体体积为
$$
 V_{x} = \pi \int_{a}^{b} f^{2}(x) dx
$$
（2）区域$ D $绕$ y $轴旋转一周所得到的旋转体体积为
$$
V_{y} = 2\pi \int_{a}^{b} x f(x) dx
$$
更一般地，
$$
V=2\pi\iint_Dr(x,y)\ d\sigma\\
r(x,y)=\frac{|ax+by+c|}{\sqrt{a^2+b^2}}
$$

#### 3.曲线弧长

（1）若曲线$ C: y = y(x) $，$ a \leq x \leq b $，则弧长$ s $为： 
$$
s = \int_{a}^{b} \sqrt{1 + y'^{2}} dx
$$
（2）若曲线$ C: \begin{cases} x = x(t) \\ y = y(t) \end{cases} $，$ \alpha \leq t \leq \beta $，则弧长$ s $为：
$$
s = \int_{\alpha}^{\beta} \sqrt{x'^{2} + y'^{2}} dt
$$
（3）若曲线$ C: r = r(\theta) $，$ \alpha \leq \theta \leq \beta $，则弧长$ s $为：
$$
 s = \int_{\alpha}^{\beta} \sqrt{r^{2} + r'^{2}} d\theta
$$

#### 4.旋转体侧面积

**微元法**

曲线$ y = f(x) $（$ f(x) \geq 0 $）和直线$ x = a $，$ x = b $（$ 0 \leq a < b $）及$ x $轴所围成区域绕$ x $轴旋转所得旋转体的侧面积为
$$
 S = 2\pi \int_{a}^{b} f(x) \sqrt{1 + f'^{2}(x)} dx
$$
用$ds$

### 二、物理应用

1. 压力
2. 变力做功
3. 引力（不常考）