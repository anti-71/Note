# 第十二章 多元积分学及其应用

## 第一节 三重积分

### 一、基础知识

#### 1.定义

$$
 \iiint_{\Omega} f(x,y,z) \, dv = \lim_{\lambda \to 0} \sum_{i=1}^{n} f(\xi_i, \eta_i, \zeta_i) \Delta v_i 
$$

#### 2.性质

与二重积分类似

#### 3.计算

##### 1）直角坐标

###### 先一后二（先单后重）

设平行于$z$且穿过闭区域$\Omega$内部的直线与$\Omega$的边界曲面$S$最多两个交点，$\Omega$在$xOy$面上的投影域为$D_{xy}$，则
$$
 \iiint_{\Omega} f(x,y,z) \, dv = \iint_{D_{xy}} dxdy \int_{z_1(x,y)}^{z_2(x,y)} f(x,y,z) \, dz 
$$

###### 先二后一（先重后单）

设空间区域$\Omega = \{(x,y,z) \mid (x,y) \in D_z, z_1 \leqslant z \leqslant z_2\}$，其中$D_z$是垂直于$z$轴的平面截闭区域$\Omega$所得的平面闭区域，则
$$
 \iiint_{\Omega} f(x,y,z) \, dv = \int_{z_1}^{z_2} dz \iint_{D_z} f(x,y,z) \, dxdy 
$$

 ##### 2）柱坐标 

柱坐标与直角坐标的关系为
$$
 \begin{cases} x = r\cos \theta, & 0 \leqslant r < +\infty, \\ y = r\sin \theta, & 0 \leqslant \theta \leqslant 2\pi, \\ z = z, & -\infty < z < +\infty \end{cases} 
$$
体积微元$dv = r dr d\theta dz$
$$
 \iiint_{\Omega} f(x,y,z) \, dv = \iiint_{\Omega} f(r\cos \theta, r\sin \theta, z) r dr d\theta dz 
$$

##### 3）球坐标 

球坐标与直角坐标的关系为
$$
\begin{cases} x = r\sin \varphi \cos \theta, & 0 \leqslant r < +\infty, \\ y = r\sin \varphi \sin \theta, & 0 \leqslant \varphi \leqslant \pi, \\ z = r\cos \varphi, & 0 \leqslant \theta \leqslant 2\pi \end{cases} 
$$
体积微元$dv = r^2 \sin \varphi dr d\varphi d\theta$
$$
 \iiint_{\Omega} f(x,y,z) \, dv = \iiint_{\Omega} f(r\sin \varphi \cos \theta, r\sin \varphi \sin \theta, r\cos \varphi) r^2 \sin \varphi dr d\varphi d\theta 
$$

 ##### 4）利用奇偶性 

若积分域$\Omega$关于$xOy$坐标平面对称，$f(x,y,z)$关于$z$有奇偶性，则
$$
 \iiint_{\Omega} f(x,y,z) \, dv = \begin{cases} 2 \iiint_{\Omega_{z \geqslant 0}} f(x,y,z) \, dv, & f(x,y,z) \text{ 关于 } z \text{ 是偶函数}, \\ 0, & f(x,y,z) \text{ 关于 } z \text{ 是奇函数}. \end{cases} 
$$

##### 5）利用变量的轮换对称性

具体应用参见例题

### 二、例题

##### 例 1

> 设$\Omega = \{(x,y,z) \mid x^2 + y^2 + z^2 \leqslant 1\}$，则$\iiint_{\Omega} z^2 \, dxdydz = \underline{\quad\quad}$

**法一**

先二后一
$$
\begin{align*}
原式  &= \int_{-1}^{1} dx \int_{D_z} dxdy \quad(  D_z: x^2 + y^2 \leq 1 - z^2 ) \\ 
&= \int_{-1}^{1} x^2 \pi (1 - z^2) dz \\
&= 2 \int_{0}^{1} \pi x^2 (1 - x^2) dx \\
&= \frac{4}{15}\pi
\end{align*}
$$

> 使用先二后一的时机：
>
> 1. 被积函数单纯关于$z$
> 2. 区域$D_z$的面积好求

**法二**

球坐标+轮换对称性计算
$$
\begin{align*}
原式 & = \frac{1}{3} \iiint (x^2 + y^2 + z^2) dV \\& = \frac{1}{3} \int_{0}^{2\pi} d\theta \int_{0}^{\pi} \sin\varphi d\varphi \int_{0}^{1} r^2 \cdot r^2 dr \\
&= \frac{4}{15}\pi 
\end{align*}
$$

##### 例 2

> 计算三重积分$\iiint_{\Omega} (x + z) dv$，其中$\Omega$是由曲面$z = \sqrt{x^2 + y^2}$与$z = \sqrt{1 - x^2 - y^2}$所围成的区域

**法一**

由$\Omega$关于$yOz$坐标平面对称，$x$是$x$的奇函数，则$\iiint_{\Omega} x dv = 0$

利用球面坐标计算$\iiint_{\Omega} z dv$： 
$$
\begin{align*}
\iiint_{\Omega} z dv &= \int_{0}^{2\pi} d\theta \int_{0}^{\frac{\pi}{4}} d\varphi \int_{0}^{1} r\cos \varphi \cdot r^2 \sin \varphi dr \\
&= 2\pi \cdot \frac{1}{2} \sin^2 \varphi \bigg|_{0}^{\frac{\pi}{4}} \cdot \frac{1}{4} \\
&= \frac{\pi}{8}
\end{align*}
$$
**法二**

$\iiint_{\Omega} x dv = 0$，
$$
\begin{align*} \iiint_{\Omega} z dv &= \iint_{x^2 + y^2 \leqslant \frac{1}{2}} dxdy \int_{\sqrt{x^2 + y^2}}^{\sqrt{1 - x^2 - y^2}} z dz \\ &= \frac{1}{2} \iint_{x^2 + y^2 \leqslant \frac{1}{2}} \left[ (1 - x^2 - y^2) - (x^2 + y^2) \right] dxdy \\ &= \frac{\pi}{4} - \iint_{x^2 + y^2 \leqslant \frac{1}{2}} (x^2 + y^2) dxdy \\ &= \frac{\pi}{4} - \int_{0}^{2\pi} d\theta \int_{0}^{\frac{\sqrt{2}}{2}} r^3 dr \\
&= \frac{\pi}{4} - \frac{\pi}{8} \\
&= \frac{\pi}{8} \end{align*}
$$
**法三**

$\iiint_{\Omega} x dv = 0$， 
$$
 \begin{align*} \iiint_{\Omega} z dv &= \int_{0}^{\frac{\sqrt{2}}{2}} dz \iint_{x^2 + y^2 \leqslant z^2} z dxdy + \int_{\frac{\sqrt{2}}{2}}^{1} dz \iint_{x^2 + y^2 \leqslant 1 - z^2} z dxdy \\ &= \pi \int_{0}^{\frac{\sqrt{2}}{2}} z^3 dz + \pi \int_{\frac{\sqrt{2}}{2}}^{1} z(1 - z^2) dz \\ &= \frac{\pi}{16} + \frac{\pi}{16} \\
 &= \frac{\pi}{8} \end{align*} 
$$
**法四**

$\iiint_{\Omega} x dv = 0$，
$$
\begin{align*} \iiint_{\Omega} z dv &= \int_{0}^{2\pi} d\theta \int_{0}^{\frac{\pi}{4}} dr \int_{r}^{\sqrt{1 - r^2}} zr dz \\
 &= \pi \int_{0}^{\frac{\pi}{4}} r(1 - 2r^2) dr \\ &= \frac{\pi}{8} \end{align*}
$$

## 第二节 曲线积分

### 一、对弧长的线积分（第一类线积分）

#### 1.定义

$$
\int_L f(x,y) \mathrm{d}s = \lim_{\lambda \to 0} \sum_{i=1}^n f(\xi_i, \eta_i) \Delta s_i
$$

#### 2.性质

$$
\int_{\widehat{L(AB)}} f(x,y) \mathrm{d}s = \int_{\widehat{L(BA)}} f(x,y) \mathrm{d}s 
$$

与积分路径方向无关

#### 3.计算方法（平面）

##### 1）直接法

1. 若$L:\begin{cases}x = x(t) \\ y = y(t)\end{cases}, \alpha \leqslant t \leqslant \beta$，则
   $$
   \int_L f(x,y) \mathrm{d}s = \int_\alpha^\beta f\left[ x(t), y(t) \right] \sqrt{x'^2(t) + y'^2(t)} \mathrm{d}t\\
   $$
   小的在积分下限，大的在积分上限

2. 若$L: y = y(x), a \leqslant x \leqslant b$，则
   $$
   \int_L f(x,y) \mathrm{d}s = \int_a^b f\left[ x, y(x) \right] \sqrt{1 + y'^2(x)} \mathrm{d}x
   $$

3. 若$L: r = r(\theta), \alpha \leqslant \theta \leqslant \beta$，则
   $$
   \int_L f(x,y) \mathrm{d}s = \int_\alpha^\beta f\left( r\cos\theta, r\sin\theta \right) \sqrt{r^2 + r'^2} \mathrm{d}\theta
   $$

 ##### 2）利用奇偶性

1. 若积分曲线$L$关于$y$轴对称，则
   $$
   \int_L f(x,y) \mathrm{d}s = \begin{cases} 2\int_{L_{x \geqslant 0}} f(x,y) \mathrm{d}s, & \text{当 } f(x,y) \text{ 关于 } x \text{ 为偶函数} \\ 0, & \text{当 } f(x,y) \text{ 关于 } x \text{ 为奇函数} \end{cases}
   $$

2. 若积分曲线$L$关于$x$轴对称，则
   $$
   \int_L f(x,y) \mathrm{d}s = \begin{cases} 2\int_{L_{y \geqslant 0}} f(x,y) \mathrm{d}s, & \text{当 } f(x,y) \text{ 关于 } y \text{ 为偶函数} \\ 0, & \text{当 } f(x,y) \text{ 关于 } y \text{ 为奇函数} \end{cases}
   $$

 ##### 3）利用对称性 

若积分曲线关于直线$y = x$对称，则
$$
 \int_L f(x,y) \mathrm{d}s = \int_L f(y,x) \mathrm{d}s 
$$
特别地，$\int_L f(x) \mathrm{d}s = \int_L f(y) \mathrm{d}s$

---

 对空间线积分$\int_L f(x,y,z) \mathrm{d}s$，通常化为定积分计算，即若曲线$L$的方程为：$x = x(t), y = y(t), z = z(t) \ (\alpha \leqslant t \leqslant \beta)$，则
$$
 \int_L f(x,y,z) \mathrm{d}s = \int_\alpha^\beta f\left[ x(t), y(t), z(t) \right] \sqrt{x'^2(t) + y'^2(t) + z'^2(t)} \mathrm{d}t 
$$

### 二、对坐标的线积分（第二类线积分）

#### 1.定义

$$
\int_L P(x,y) \mathrm{d}x + Q(x,y) \mathrm{d}y = \lim_{\lambda \to 0} \sum_{i=1}^n \left[ P(\xi_i, \eta_i) \Delta x_i + Q(\xi_i, \eta_i) \Delta y_i \right]
$$

#### 2.性质

$$
\int_{\widehat{L(AB)}} P \mathrm{d}x + Q \mathrm{d}y = -\int_{\widehat{L(BA)}} P \mathrm{d}x + Q \mathrm{d}y 
$$

与积分路径方向有关

#### 3.计算方法（平面）

##### 1）直接法

 设光滑曲线$L:\begin{cases}x = x(t) \\ y = y(t)\end{cases}, t \in [\alpha, \beta]$，则
$$
 \int_L P \mathrm{d}x + Q \mathrm{d}y = \int_\alpha^\beta \left\{ P\left[ x(t), y(t) \right] x'(t) + Q\left[ x(t), y(t) \right] y'(t) \right\} \mathrm{d}t 
$$
起点为积分下限，终点为积分上限

##### 2）格林公式 

设闭区域$D$由分段光滑的曲线$L$围成，函数$P(x,y)$、$Q(x,y)$在$D$上具有一阶连续偏导数，则有
$$
 \oint_L P \mathrm{d}x + Q \mathrm{d}y = \iint_D \left( \frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y} \right) \mathrm{d}\sigma 
$$
其中$L$为$D$取**正向**的边界曲线

##### 3）补线用格林公式（利用线积分与路径无关）

###### 线积分与路径无关的判定

**定理**	设函数$P(x,y)$、$Q(x,y)$在**单连通**域$D$上有一阶连续偏导数，则以下四条等价：

1. 线积分
   $$
    \int_L P \mathrm{d}x + Q \mathrm{d}y 
   $$
   与路径无关

2. $$
    \oint_L P \mathrm{d}x + Q \mathrm{d}y = 0,
   $$

   其中$L$为$D$中任一分段光滑闭曲线

3. $$
   \frac{\partial P}{\partial y} = \frac{\partial Q}{\partial x}, \forall (x,y) \in D
   $$

4. $$
   P(x,y)\mathrm{d}x + Q(x,y)\mathrm{d}y = \mathrm{d}F(x,y)
   $$

 ###### 计算

1. 改换路径计算  

   一般是沿平行于坐标轴的直线积分：
   $$
   \int_{(x_1,y_1)}^{(x_2,y_2)} P \mathrm{d}x + Q \mathrm{d}y = \int_{x_1}^{x_2} P(x, y_1) \mathrm{d}x + \int_{y_1}^{y_2} Q(x_2, y) \mathrm{d}y
   $$
   或
   $$
   \int_{(x_1,y_1)}^{(x_2,y_2)} P \mathrm{d}x + Q \mathrm{d}y = \int_{y_1}^{y_2} Q(x_1, y) \mathrm{d}y + \int_{x_1}^{x_2} P(x, y_2) \mathrm{d}x
   $$

2. 利用原函数计算  

   设$P\mathrm{d}x + Q\mathrm{d}y = \mathrm{d}F(x,y)$，即$F(x,y)$为$P\mathrm{d}x + Q\mathrm{d}y$的原函数，则
   $$
    \int_{(x_1,y_1)}^{(x_2,y_2)} P \mathrm{d}x + Q \mathrm{d}y = F(x_2, y_2) - F(x_1, y_1) 
   $$
   求原函数**方法**：偏积分、凑微分

#### 4.两类线积分的联系

$$
\oint_L P \mathrm{d}x + Q \mathrm{d}y = \oint_L (P \cos \alpha + Q \cos \beta) \mathrm{d}s
$$

其中$\cos \alpha$、$\cos \beta$是切线方向的方向余弦

#### 5.计算方法（空间）

##### 1）直接法 

$$
 \begin{align*} &\int_L P(x,y,z) \mathrm{d}x + Q(x,y,z) \mathrm{d}y + R(x,y,z) \mathrm{d}z \\ &= \int_\alpha^\beta \left\{ P\left[ x(t), y(t), z(t) \right] x'(t) + Q\left[ x(t), y(t), z(t) \right] y'(t) + R\left[ x(t), y(t), z(t) \right] z'(t) \right\} \mathrm{d}t \end{align*} 
$$

##### 2）斯托克斯公式 

$$
 \begin{align*} &\oint_L P(x,y,z) \mathrm{d}x + Q(x,y,z) \mathrm{d}y + R(x,y,z) \mathrm{d}z \\ &= \iint_\Sigma \begin{vmatrix} \cos\alpha & \cos\beta & \cos\gamma \\ \frac{\partial}{\partial x} & \frac{\partial}{\partial y} & \frac{\partial}{\partial z} \\ P & Q & R \end{vmatrix} \mathrm{d}S \\ &= \iint_\Sigma \left( \frac{\partial R}{\partial y} - \frac{\partial Q}{\partial z} \right) \mathrm{d}y\mathrm{d}z + \left( \frac{\partial P}{\partial z} - \frac{\partial R}{\partial x} \right) \mathrm{d}z\mathrm{d}x + \left( \frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y} \right) \mathrm{d}x\mathrm{d}y \end{align*}
$$

### 三、例题

#### 第一类线积分的计算

#### 例 1

> 设$L$为椭圆$\frac{x^2}{4} + \frac{y^2}{3} = 1$，其周长记为$a$，则$\oint_L (2xy + 3x^2 + 4y^2)\mathrm{d}s = \underline{\quad\quad}$

由奇偶性知
$$
 \oint_L 2xy\mathrm{d}s = 0 
$$

$$
\begin{align*}
原式 &= \oint_L (3x^2 + 4y^2)\mathrm{d}s \\
&= 12\oint_L \left( \frac{x^2}{4} + \frac{y^2}{3} \right)\mathrm{d}s \\ 
&= 12\oint_L 1\mathrm{d}s \\
&= 12a 
\end{align*}
$$

#### 第二类线积分的计算

##### 例 2

> 计算曲线积分$\int_L \sin 2x \mathrm{d}x + 2(x^2 - 1)y \mathrm{d}y$，其中$L$是曲线$y = \sin x$上从点$(0,0)$到点$(\pi,0)$的一段

**法一**

直接法
$$
\begin{align*} \int_L \sin 2x \mathrm{d}x + 2(x^2 - 1)y \mathrm{d}y &= \int_0^\pi \left[ \sin 2x + 2(x^2 - 1)\sin x \cdot \cos x \right] \mathrm{d}x \\ &= \int_0^\pi x^2 \mathrm{d}(\sin^2 x) \\ &= \left. x^2 \sin^2 x \right|_0^\pi - 2\int_0^\pi x \sin^2 x \mathrm{d}x \\ &= (-2) \cdot \frac{\pi}{2} \int_0^\pi \sin^2 x \mathrm{d}x \\ &= -\pi \int_0^\pi \sin^2 x \mathrm{d}x \\ &= -\pi \cdot 2\int_0^{\frac{\pi}{2}} \sin^2 x \mathrm{d}x \\ &= (-2\pi) \cdot \frac{1}{2} \cdot \frac{\pi}{2} \\ &= -\frac{\pi^3}{2} \end{align*}
$$
**法二**

补线用格林公式 

补线段$L_1: y = 0$（$x$从$\pi$到$0$），使$L + L_1$围成闭区域$D$

设$P = \sin 2x$，$Q = 2(x^2 - 1)y$，则$\frac{\partial Q}{\partial x} = 4xy$，$\frac{\partial P}{\partial y} = 0$

由格林公式：
$$
\oint_{L + L_1} P \mathrm{d}x + Q \mathrm{d}y = \iint_D \left( \frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y} \right) \mathrm{d}\sigma = \iint_D 4xy \mathrm{d}\sigma 
$$
由于区域$D$关于$x$轴对称，被积函数$4xy$是关于$y$的奇函数，故$\iint_D 4xy \mathrm{d}\sigma = 0$，即：
$$
 \int_L P \mathrm{d}x + Q \mathrm{d}y + \int_{L_1} P \mathrm{d}x + Q \mathrm{d}y = 0 
$$
计算$\int_{L_1} P \mathrm{d}x + Q \mathrm{d}y$（$y = 0$，$\mathrm{d}y = 0$）： 
$$
 \int_{L_1} \sin 2x \mathrm{d}x + 2(x^2 - 1) \cdot 0 \cdot \mathrm{d}y = \int_\pi^0 \sin 2x \mathrm{d}x = \left. -\frac{1}{2}\cos 2x \right|_\pi^0 = -\frac{1}{2}(1 - 1) = 0 
$$
因此：
$$
 \int_L P \mathrm{d}x + Q \mathrm{d}y = 0 - 0 = -\frac{\pi^3}{2} 
$$

##### 例 3

> 求$I = \int_L \left[ \mathrm{e}^x \sin y - b(x + y) \right] \mathrm{d}x + \left( \mathrm{e}^x \cos y - ax \right) \mathrm{d}y$，其中$a,b$为正的常数，$L$为从点$A(2a,0)$沿曲线$y = \sqrt{2ax - x^2}$到点$O(0,0)$的弧

**法一**

添加从点$O(0,0)$沿$y = 0$到点$A(2a,0)$的有向直线段$L_1$，则
$$
 I = \int_{L \cup L_1} \left[ \mathrm{e}^x \sin y - b(x + y) \right] \mathrm{d}x + \left( \mathrm{e}^x \cos y - ax \right) \mathrm{d}y - \int_{L_1} \left[ \mathrm{e}^x \sin y - b(x + y) \right] \mathrm{d}x + \left( \mathrm{e}^x \cos y - ax \right) \mathrm{d}y 
$$
由格林公式可知，前一积分
$$
 I_1 = \iint_D (b - a) \mathrm{d}\sigma = \frac{\pi}{2}a^2(b - a) 
$$
其中$D$为$L \cup L_1$所围成的半圆域

直接计算后一积分可得
$$
 I_2 = \int_0^{2a} (-bx) \mathrm{d}x = -2a^2b 
$$
从而$I = I_1 - I_2 = \frac{\pi}{2}a^2(b - a) + 2a^2b = \left( \frac{\pi}{2} + 2 \right)a^2b - \frac{\pi}{2}a^3$

**法二**
$$
 \begin{align*} I &= \int_L \left( \mathrm{e}^x \sin y - b(x + y) \right) \mathrm{d}x + \left( \mathrm{e}^x \cos y - ax \right) \mathrm{d}y \\ &= \int_L \mathrm{e}^x \sin y \mathrm{d}x + \mathrm{e}^x \cos y \mathrm{d}y - \int_L b(x + y) \mathrm{d}x + ax \mathrm{d}y \end{align*} 
$$
前一积分与路径无关，所以
$$
 \int_L \mathrm{e}^x \sin y \mathrm{d}x + \mathrm{e}^x \cos y \mathrm{d}y = \left. \mathrm{e}^x \sin y \right|_{(2a,0)}^{(0,0)} = 0
$$
对后一积分，取参数方程：$\begin{cases} x = a + a\cos t, \\ y = a\sin t, \end{cases}$
$$
\begin{align*} &\int_L b(x + y) \mathrm{d}x + ax \mathrm{d}y \\ &= \int_0^\pi \left( -a^2 b \sin t - a^2 b \sin t \cos t - a^2 b \sin^2 t + a^3 \cos t + a^3 \cos^2 t \right) \mathrm{d}t \\ &= -2a^2 b - \frac{1}{2}\pi a^2 b + \frac{1}{2}\pi a^3 \end{align*}
$$

##### 例 4

> 设$L$是柱面$x^2 + y^2 = 1$与平面$y + z = 0$的交线，从$z$轴正向往$z$轴负向看去为逆时针方向，则曲线积分$\oint_L z \mathrm{d}x + y \mathrm{d}z = \underline{\quad\quad}$

**法一**

参数方程法 

柱面$x^2 + y^2 = 1$与平面$y + z = 0$的交线的参数方程为 
$$
\begin{cases} x = \cos t, \\ y = \sin t, \\ z = -\sin t, \end{cases} \quad (0 \leqslant t \leqslant 2\pi)
$$
则
$$
\begin{align*} \oint_L z \mathrm{d}x + y \mathrm{d}z &= \int_0^{2\pi} \left( \sin^2 t - \sin t \cos t \right) \mathrm{d}t \\ &= 4 \int_0^{\frac{\pi}{2}} \sin^2 t \mathrm{d}t \\ &= 4 \cdot \frac{1}{2} \cdot \frac{\pi}{2} \\ &= \pi \end{align*}
$$
**法二**

斯托克斯公式法 

由斯托克斯公式得
$$
 \oint_L z \mathrm{d}x + y \mathrm{d}z = \iint_\Sigma (1 - 0)\mathrm{d}y\mathrm{d}z + (1 - 0)\mathrm{d}z\mathrm{d}x + (0 - 0)\mathrm{d}x\mathrm{d}y 
$$
其中$\Sigma$为平面$y + z = 0$包含在柱面$x^2 + y^2 = 1$之内部分的**上侧**，因此
$$
\begin{align*} \oint_L z \mathrm{d}x + y \mathrm{d}z &= \iint_\Sigma \mathrm{d}y\mathrm{d}z + \mathrm{d}z\mathrm{d}x \\ &= \iint_{D_{zx}} \mathrm{d}z\mathrm{d}x \quad (\text{其中} \ D_{zx} = \{(x,z) \mid x^2 + z^2 \leqslant 1\}) \\ &= \pi \end{align*}
$$
**法三**

化为平面线积分（格林公式法）

由$y + z = 0$得$z = -y$，代入积分$\oint_L z \mathrm{d}x + y \mathrm{d}z$得
$$
 \oint_L z \mathrm{d}x + y \mathrm{d}z = \oint_C (-y) \mathrm{d}x - y \mathrm{d}y \quad (\text{其中} \ C \text{为单位圆} \ x^2 + y^2 = 1 \ \text{沿逆时针方向}) 
$$
由格林公式：
$$
 \oint_C (-y) \mathrm{d}x - y \mathrm{d}y = \iint_{x^2 + y^2 \leqslant 1} \mathrm{d}x\mathrm{d}y = \pi
$$
法三简便，因为平面$y+z=0$较简单

## 第三节 曲面积分

### 一、对面积的面积分（第一类面积分）

#### 1.定义

$$
\iint_{\Sigma} f(x, y, z) \mathrm{d} S = \lim_{\lambda \to 0} \sum_{i=1}^{n} f(\xi_{i}, \eta_{i}, \zeta_{i}) \Delta S_{i}
$$

#### 2.性质

$$
\iint_{\Sigma} f(x, y, z) \mathrm{d} S = \iint_{\Sigma} f(x, y, z) \mathrm{d} S
$$

与积分曲面的方向无关

#### 3.计算

##### 1）直接法 

设曲面$\Sigma: z = z(x, y)$，$(x, y) \in D_{xy}$，则
$$
 \iint_{\Sigma} f(x, y, z) \mathrm{d}S = \iint_{D_{xy}} f\left[x, y, z(x, y)\right] \sqrt{1 + (z_x')^2 + (z_y')^2} \mathrm{d}x\mathrm{d}y 
$$
若曲面由方程$x = x(y, z)$或$y = y(z, x)$给出，也可类似地把对面积的面积分化为相应的二重积分

##### 2）利用奇偶性 

若曲面$\Sigma$关于$xOy$面对称，则
$$
 \iint_{\Sigma} f(x, y, z) \mathrm{d}S =  \begin{cases}  2\iint_{\Sigma_{\geq 0}} f(x, y, z) \mathrm{d}S, & \text{当 } f(x, y, z) \text{ 关于 } z \text{ 为偶函数}, \\ 0, & \text{当 } f(x, y, z) \text{ 关于 } z \text{ 为奇函数} \end{cases} 
$$
若曲面$\Sigma$关于$yOz$面或$zOx$面对称，也有类似的结论

##### 3）利用对称性 

具体应用参见例题

### 二、对坐标的面积分（第二类面积分）

#### 1.定义

$$
\iint_{\Sigma} R(x, y, z) \mathrm{d}x\mathrm{d}y = \lim_{\lambda \to 0} \sum_{i=1}^{n} R(\xi_{i}, \eta_{i}, \zeta_{i}) (\Delta S_{i})_{xy}
$$

#### 2.性质

$$
\iint_{\Sigma} P\mathrm{d}y\mathrm{d}z + Q\mathrm{d}z\mathrm{d}x + R\mathrm{d}x\mathrm{d}y = -\iint_{-\Sigma} P\mathrm{d}y\mathrm{d}z + Q\mathrm{d}z\mathrm{d}x + R\mathrm{d}x\mathrm{d}y
$$

与积分曲面的方向有关

#### 3.计算

##### 1）直接法

设有向曲面$\Sigma: z = z(x, y)$，$(x, y) \in D_{xy}$，则
$$
  \iint_{\Sigma} R(x, y, z) \mathrm{d}x\mathrm{d}y = \pm \iint_{D_{xy}} R\left[x, y, z(x, y)\right] \mathrm{d}x\mathrm{d}y  
$$
若有向曲面$\Sigma$的法线向量与$z$轴正向夹角为锐角（即曲面上侧），取正号；否则取负号

设有向曲面$\Sigma: x = x(y, z)$，$(y, z) \in D_{yz}$，则
$$
 \iint_{\Sigma} P(x, y, z) \mathrm{d}y\mathrm{d}z = \pm \iint_{D_{yz}} P\left[x(y, z), y, z\right] \mathrm{d}y\mathrm{d}z  
$$
若有向曲面$\Sigma$的法线向量与$x$轴正向夹角为锐角（即曲面前侧），取正号；否则取负号

设有向曲面$\Sigma: y = y(z, x)$，$(z, x) \in D_{zx}$，则
$$
 \iint_{\Sigma} Q(x, y, z) \mathrm{d}z\mathrm{d}x = \pm \iint_{D_{zx}} Q\left[x, y(z, x), z\right] \mathrm{d}z\mathrm{d}x  
$$
若有向曲面$\Sigma$的法线向量与$y$轴正向夹角为锐角（即曲面右侧），取正号；否则取负号 

##### 2）高斯公式 

**空间闭区域**$\Omega$
$$
 \oiint_{\Sigma_{\text{外}}} P\mathrm{d}y\mathrm{d}z + Q\mathrm{d}z\mathrm{d}x + R\mathrm{d}x\mathrm{d}y = \iiint_{\Omega} \left( \frac{\partial P}{\partial x} + \frac{\partial Q}{\partial y} + \frac{\partial R}{\partial z} \right) \mathrm{d}v 
$$

##### 3）补面用高斯公式

 具体应用参见例题

#### 4. 两类面积分的联系

$$
 \iint_{\Sigma} (P\cos\alpha + Q\cos\beta + R\cos\gamma) \mathrm{d}S = \iint_{\Sigma} (P\mathrm{d}y\mathrm{d}z + Q\mathrm{d}z\mathrm{d}x + R\mathrm{d}x\mathrm{d}y) 
$$

其中$\cos \alpha$、$\cos \beta$、$\cos \gamma$是切平面方向的方向余弦

### 三、例题

#### 第一类曲面积分的计算

##### 例 1

> 计算曲面积分$\iint_{\Sigma} z \mathrm{d}S$，其中$\Sigma$为锥面$z = \sqrt{x^2 + y^2}$在柱体$x^2 + y^2 \leqslant 2x$内的部分

$\Sigma$在$xOy$平面上的投影区域$D: x^2 + y^2 \leqslant 2x$

由$z = \sqrt{x^2 + y^2}$，求偏导得$z_x' = \frac{x}{\sqrt{x^2 + y^2}}$，$z_y' = \frac{y}{\sqrt{x^2 + y^2}}$，则
$$
 \mathrm{d}S = \sqrt{1 + (z_x')^2 + (z_y')^2} \mathrm{d}\sigma = \sqrt{2} \mathrm{d}\sigma 
$$
于是
$$
 \begin{align*} \iint_{\Sigma} z \mathrm{d}S &= \iint_{D} \sqrt{x^2 + y^2} \cdot \sqrt{2} \mathrm{d}\sigma \\ &= \sqrt{2} \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} \mathrm{d}\theta \int_{0}^{2\cos\theta} r^2 \mathrm{d}r \\ &= \frac{16\sqrt{2}}{3} \int_{0}^{\frac{\pi}{2}} \cos^3\theta \mathrm{d}\theta \\ &= \frac{32}{9} \sqrt{2} \end{align*} 
$$

#### 第二类曲面积分的计算

##### 例 2

> 设曲面$\Sigma$是$z = \sqrt{4 - x^2 - y^2}$的上侧，则$\iint_{\Sigma} xy\mathrm{d}y\mathrm{d}z + x\mathrm{d}z\mathrm{d}x + x^2\mathrm{d}x\mathrm{d}y = \underline{\quad\quad}$

设$\Sigma_1$是曲面$z = 0$（$x^2 + y^2 \leqslant 4$）取下侧，由$\Sigma$与$\Sigma_1$所围的立体（上半球体）为$\Omega$，则由高斯公式可知
$$
 \iint_{\Sigma} = \iint_{\Sigma + \Sigma_1} - \iint_{\Sigma_1} = \iiint_{\Omega} y\mathrm{d}x\mathrm{d}y\mathrm{d}z - \iint_{\Sigma_1}
$$
由对称性知$\iiint_{\Omega} y\mathrm{d}x\mathrm{d}y\mathrm{d}z = 0$，故
$$
 \begin{align*} \iint_{\Sigma} &= - \iint_{\Sigma_1} = \iint_{x^2 + y^2 \leqslant 4} x^2\mathrm{d}x\mathrm{d}y \\ &= \frac{1}{2} \iint_{x^2 + y^2 \leqslant 4} (x^2 + y^2)\mathrm{d}x\mathrm{d}y \\ &= 4\pi \end{align*} 
$$

##### 例 3

> 设$\Sigma$是曲面$z = x^2 + y^2$（$z \leqslant 1$）的上侧，计算曲面积分
> $$
> I = \iint_{\Sigma} (x-1)^3\mathrm{d}y\mathrm{d}z + (y-1)^3\mathrm{d}z\mathrm{d}x + (z-1)\mathrm{d}x\mathrm{d}y
> $$

设$S$为平面$z = 1$包含在曲面$z = x^2 + y^2$之内部分的下侧，则
$$
\begin{align*} I &= \oiint_{\Sigma + S} (x-1)^3\mathrm{d}y\mathrm{d}z + (y-1)^3\mathrm{d}z\mathrm{d}x + (z-1)\mathrm{d}x\mathrm{d}y - \iint_{S} (x-1)^3\mathrm{d}y\mathrm{d}z + (y-1)^3\mathrm{d}z\mathrm{d}x + (z-1)\mathrm{d}x\mathrm{d}y \\ &= -\iiint_{\Omega} \left[ 3(x-1)^2 + 3(y-1)^2 + 1 \right] \mathrm{d}v - 0 \quad (\Omega \text{ 为 } z = 1 \text{ 和 } z = x^2 + y^2 \text{ 所围空间体}) \\ &= -\iiint_{\Omega} (3x^2 + 3y^2 + 7)\mathrm{d}v + 6\iiint_{\Omega} x\mathrm{d}v + 6\iiint_{\Omega} y\mathrm{d}v, \end{align*} 
$$
 注意到$\Omega$关于$yOz$面和$zOx$面都对称，则
$$
\iiint_{\Omega} x\mathrm{d}v = \iiint_{\Omega} y\mathrm{d}v = 0
$$
用柱坐标计算$\iiint_{\Omega} (3x^2 + 3y^2 + 7)\mathrm{d}v$：
$$
 \begin{align*} \iiint_{\Omega} (3x^2 + 3y^2 + 7)\mathrm{d}v &= \int_{0}^{2\pi} \mathrm{d}\theta \int_{0}^{1} r\mathrm{d}r \int_{r^2}^{1} (3r^2 + 7)\mathrm{d}z \\ &= 2\pi \int_{0}^{1} r(3r^2 + 7)(1 - r^2)\mathrm{d}r \\ &= 4\pi, \end{align*} 
$$
故$I = -4\pi$

## 第四节 多元积分应用

### 一、基础知识

**多元积分应用一览表**

![](D:\Note\考研\高等数学\Assets\多元积分应用一览表.png)

**多元积分应用一览表**

#### 1.变力做功

$$
力\boldsymbol{F} = P\boldsymbol{i} + Q\boldsymbol{j} + R\boldsymbol{k}，\\\ W = \int_{\overparen{AB}} P\mathrm{d}x + Q\mathrm{d}y + R\mathrm{d}z
$$

#### 2.通量

$$
向量场:\boldsymbol{U}(x,y,z) = P\boldsymbol{i} + Q\boldsymbol{j} + R\boldsymbol{k}\\通量:\varPhi = \iint_{\Sigma} P\mathrm{d}y\mathrm{d}z + Q\mathrm{d}z\mathrm{d}x + R\mathrm{d}x\mathrm{d}y
$$

### 二、例题

##### 例 1

> 设有一半径为$R$的球体，$P_0$是此球的表面上的一个定点，球体上任一点的密度与该点到$P_0$距离的平方成正比（比例常数$k > 0$），求球体的重心位置

设所考虑的球体为$\Omega$、球心为$O$，以定点$P_0$为原点，射线$P_0O$为正$z$轴建立直角坐标系，则球面的方程为
$$
 x^2 + y^2 + z^2 = 2Rz
$$
设$\Omega$的重心位置为$(\bar{x}, \bar{y}, \bar{z})$，由对称性，得
$$
 \bar{x} = 0, \bar{y} = 0, \bar{z} = \frac{\iiint_{\Omega} kz(x^2 + y^2 + z^2) \mathrm{d}v}{\iiint_{\Omega} k(x^2 + y^2 + z^2) \mathrm{d}v}
$$
计算积分：
$$
\begin{align*} \iiint_{\Omega} (x^2 + y^2 + z^2) \mathrm{d}v &= 4 \int_{0}^{\frac{\pi}{2}} \mathrm{d}\theta \int_{0}^{\frac{\pi}{2}} \mathrm{d}\varphi \int_{0}^{2R\cos\varphi} r^4 \sin\varphi \mathrm{d}r \\ &= \frac{32}{15}\pi R^5, \end{align*} 
$$

$$
 \begin{align*} \iiint_{\Omega} z(x^2 + y^2 + z^2) \mathrm{d}v &= 4 \int_{0}^{\frac{\pi}{2}} \mathrm{d}\theta \int_{0}^{\frac{\pi}{2}} \mathrm{d}\varphi \int_{0}^{2R\cos\varphi} r^5 \sin\varphi \cos\varphi \mathrm{d}r \\ &= \frac{64}{3}\pi R^6 \int_{0}^{\frac{\pi}{2}} \cos^7\varphi \sin\varphi \mathrm{d}\varphi \\ &= \frac{8}{3}\pi R^6, \end{align*} 
$$

故$\bar{z} = \frac{5}{4}R$

因此，球体$\Omega$的重心位置为$\left( 0, 0, \frac{5}{4}R \right)$

##### 例 2

> 质点$P$沿着以$AB$为直径的半圆周，从点$A(1,2)$运动到点$B(3,4)$的过程中受到变力$\boldsymbol{F}$作用，$\boldsymbol{F}$的大小等于点$P$与原点$O$之间的距离，其方向垂直于直线段$OP$，且与$y$轴正向的夹角小于$\frac{\pi}{2}$
>
> ![](D:\Note\考研\高等数学\Assets\第十二章例2.png)
>
> 求变力$\boldsymbol{F}$对质点$P$所做的功

**法一**

按题意，变力$\boldsymbol{F} = -y\boldsymbol{i} + x\boldsymbol{j}$

变力$\boldsymbol{F}$所做的功
$$
W = \int_{\overparen{AB}} (-y)\mathrm{d}x + x\mathrm{d}y
$$
圆弧$\overparen{AB}$的参数方程是
$$
 \begin{cases} x = 2 + \sqrt{2}\cos\theta, \\ y = 3 + \sqrt{2}\sin\theta, \end{cases} \quad -\frac{3\pi}{4} \leqslant \theta \leqslant \frac{\pi}{4}, 
$$
则
$$
 \begin{align*} W &= \int_{\overparen{AB}} (-y)\mathrm{d}x + x\mathrm{d}y \\ &= \int_{-\frac{3\pi}{4}}^{\frac{\pi}{4}} \left[ \sqrt{2}(3 + \sqrt{2}\sin\theta)\sin\theta + \sqrt{2}(2 + \sqrt{2}\cos\theta)\cos\theta \right] \mathrm{d}\theta \\ &= 2(\pi - 1) \end{align*} 
$$
**法二**

按题意，变力$\boldsymbol{F} = -y\boldsymbol{i} + x\boldsymbol{j}$

变力$\boldsymbol{F}$所做的功
$$
 \begin{align*} W &= \int_{\overparen{AB}} (-y)\mathrm{d}x + x\mathrm{d}y \\ &= \oint_{AB + \text{BA}} (-y)\mathrm{d}x + x\mathrm{d}y - \int_{\text{BA}} (-y)\mathrm{d}x + x\mathrm{d}y \\ &= \iint_{D} 2\mathrm{d}x\mathrm{d}y - \int_{3}^{1} -(1 + x)\mathrm{d}x + x\mathrm{d}x \\ &= 2\pi - 2 \end{align*} 
$$

## 第五节 场论初步

### 一、基础知识

#### 1.方向导数

**定义**	
$$
\left. \frac{\partial f}{\partial \boldsymbol{l}} \right|_{(x_0,y_0)} = \lim_{t \to 0^+} \frac{f(x_0 + t\cos\alpha, y_0 + t\cos\beta) - f(x_0, y_0)}{t},
$$
其中$\boldsymbol{e}_l = (\cos\alpha, \cos\beta)$

**定理**	若函数$f(x,y)$在点$P_0(x_0,y_0)$处**可微**，则函数在该点沿任一方向$\boldsymbol{l}$的方向导数都存在，且有 
$$
\left. \frac{\partial f}{\partial \boldsymbol{l}} \right|_{(x_0,y_0)} = f_x'(x_0,y_0)\cos\alpha + f_y'(x_0,y_0)\cos\beta
$$

#### 2.梯度

**定义**	设$f(x,y)$在点$P_0(x_0,y_0)$处具有一阶连续偏导数，则
$$
\text{grad}f(x_0,y_0) = f_x'(x_0,y_0)\boldsymbol{i} + f_y'(x_0,y_0)\boldsymbol{j}
$$

> 梯度$\text{grad}f(x_0,y_0)$是一个向量，它的方向是函数在这点方向导数最大的方向，它的模等于方向导数的最大值

#### 3.散度

设有向量场$\boldsymbol{A}(x,y,z) = \{P,Q,R\}$，其中$P,Q,R$均具有一阶连续偏导数，则
$$
\text{div} \boldsymbol{A} = \frac{\partial P}{\partial x} + \frac{\partial Q}{\partial y} + \frac{\partial R}{\partial z}
$$

#### 4.旋度

设有向量场$\boldsymbol{A}(x,y,z) = \{P,Q,R\}$，其中$P,Q,R$均具有一阶连续偏导数，则 
$$
\text{rot} \boldsymbol{A} = \begin{vmatrix} \boldsymbol{i} & \boldsymbol{j} & \boldsymbol{k} \\ \frac{\partial}{\partial x} & \frac{\partial}{\partial y} & \frac{\partial}{\partial z} \\ P & Q & R \end{vmatrix}
$$

### 二、例题

##### 例 1

> 函数$u = \ln(x + \sqrt{y^2 + z^2})$在点$A(1,0,1)$处沿$A$指向$B(3,-2,2)$方向的方向导数为$\underline{\quad\quad}$

由于$u(x,0,1) = \ln(x + 1)$，则
$$
 \left. \frac{\partial u}{\partial x} \right|_{(1,0,1)} = \left. \frac{1}{x + 1} \right|_{x=1} = \frac{1}{2}
$$
由于$u(1,y,1) = \ln(1 + \sqrt{y^2 + 1})$为$y$的偶函数，其导函数为奇函数，因此
$$
 \left. \frac{\partial u}{\partial y} \right|_{(1,0,1)} = 0
$$
由于$u(1,0,z) = \ln(1 + z)$，则
$$
\left. \frac{\partial u}{\partial z} \right|_{(1,0,1)} = \left. \frac{1}{1 + z} \right|_{z=1} = \frac{1}{2}
$$
又$\overrightarrow{AB} = \{2, -2, 1\}$，其方向余弦为
$$
 \cos\alpha = \frac{2}{3}, \cos\beta = -\frac{2}{3}, \cos\gamma = \frac{1}{3}
$$
则所求方向导数为
$$
 \frac{1}{2} \times \frac{2}{3} + 0 \times \left(-\frac{2}{3}\right) + \frac{1}{2} \times \frac{1}{3} = \frac{1}{2}
$$

##### 例 2

> $$
> \text{grad}\left( xy + \frac{z}{y} \right)\bigg|_{(2,1,1)} = \underline{\quad\quad}
> $$

对$x$求偏导得  
$$
\frac{\partial f}{\partial x} = y
$$
对$y$求偏导得
$$
\frac{\partial f}{\partial y} = x - \frac{z}{y^2} 
$$
对$z$求偏导得 
$$
 \frac{\partial f}{\partial z} = \frac{1}{y}  
$$
 根据梯度的定义$\text{grad}f = \frac{\partial f}{\partial x}\boldsymbol{i} + \frac{\partial f}{\partial y}\boldsymbol{j} + \frac{\partial f}{\partial z}\boldsymbol{k}$，代入偏导结果得：
$$
 \text{grad}\left( xy + \frac{z}{y} \right)\bigg|_{(2,1,1)} = \boldsymbol{i} + \boldsymbol{j} + \boldsymbol{k} 
$$

##### 例 3

> 向量场$\boldsymbol{u}(x,y,z) = xy^2\boldsymbol{i} + ye^z\boldsymbol{j} + x\ln(1 + z^2)\boldsymbol{k}$在点$P(1,1,0)$处的散度$\text{div}\ \boldsymbol{u} = \underline{\quad\quad}$

根据散度的定义，若向量场$\boldsymbol{u} = \{P, Q, R\}$，则散度$\text{div}\ \boldsymbol{u} = \frac{\partial P}{\partial x} + \frac{\partial Q}{\partial y} + \frac{\partial R}{\partial z}$

对于本题，$P = xy^2$，$Q = ye^z$，$R = x\ln(1 + z^2)$，分别求偏导数：
$$
\frac{\partial P}{\partial x} = y^2\\
\frac{\partial Q}{\partial y} = e^z\\
\frac{\partial R}{\partial z} = x \cdot \frac{2z}{1 + z^2}
$$
将点$P(1,1,0)$代入上述偏导数： 
$$
\left. \frac{\partial P}{\partial x} \right|_{(1,1,0)} = 1^2 = 1\\\left. \frac{\partial Q}{\partial y} \right|_{(1,1,0)} = e^0 = 1\\\left. \frac{\partial R}{\partial z} \right|_{(1,1,0)} = 1 \cdot \frac{2 \times 0}{1 + 0^2} = 0
$$
因此，散度$\text{div}\ \boldsymbol{u}\big|_{(1,1,0)} = 1 + 1 + 0 = 2$

##### 例 4

> 设$\boldsymbol{F}(x,y,z) = xy\boldsymbol{i} - yz\boldsymbol{j} + zx\boldsymbol{k}$，则$\text{rot}\ \boldsymbol{F}(1,1,0) = \underline{\quad\quad}$

$$
 \begin{align*} \text{rot}\ \boldsymbol{F}(1,1,0) &= \begin{vmatrix} \boldsymbol{i} & \boldsymbol{j} & \boldsymbol{k} \\ \frac{\partial}{\partial x} & \frac{\partial}{\partial y} & \frac{\partial}{\partial z} \\ xy & -yz & zx \end{vmatrix}\bigg|_{(1,1,0)} \\ &= \left[(0 + y)\boldsymbol{i} - (z - 0)\boldsymbol{j} + (0 - x)\boldsymbol{k}\right]\bigg|_{(1,1,0)} \\ &= \boldsymbol{i} - \boldsymbol{k} \end{align*}
$$



