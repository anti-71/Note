---
名称: 施密特正交化与正交矩阵
章: 第五章 特征值与特征向量
节: "[[第三节 实对称矩阵]]"
tags:
  - 知识点
index: 1
---
##### 1）向量的内积 

设两个 $n$ 维列向量 $\boldsymbol{\alpha} = (a_1,a_2,\dots,a_n)^T$，$\boldsymbol{\beta} = (b_1,b_2,\dots,b_n)^T$，则称 $\boldsymbol{\alpha}^T\boldsymbol{\beta} = \boldsymbol{\beta}^T\boldsymbol{\alpha} = a_1b_1 + a_2b_2 + \dots + a_nb_n$（即其对应元素相乘后再相加得到的实数）为向量 $\boldsymbol{\alpha}$ 与 $\boldsymbol{\beta}$ 的 **内积**，记为 $(\boldsymbol{\alpha},\boldsymbol{\beta})$

当 $(\boldsymbol{\alpha},\boldsymbol{\beta}) = 0$ 时，称向量 $\boldsymbol{\alpha}$ 与 $\boldsymbol{\beta}$ **正交**

- **长度**：

$\|\boldsymbol{\alpha}\| = \sqrt{(\boldsymbol{\alpha},\boldsymbol{\alpha})} = \sqrt{a_1^2 + a_2^2 + \dots + a_n^2}$，显然 $\|\boldsymbol{\alpha}\| \geqslant 0$，且 $\|\boldsymbol{\alpha}\| = 0 \iff \boldsymbol{\alpha} = 0$

- **单位向量**：长度为 1 的向量
- **非零向量 $\boldsymbol{\alpha}$ 的单位化**：$\dfrac{1}{\|\boldsymbol{\alpha}\|}\boldsymbol{\alpha}$

##### 2）正交向量组 

一组两两正交的非零向量称为 **正交向量组**（线性无关）

##### 3）标准正交向量组 

一组两两正交的单位向量称为 **标准（规范）正交向量组**

##### 4）施密特正交化 

施密特正交化是把线性无关的向量组 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\dots,\boldsymbol{\alpha}_s$ 化为与之等价的标准正交向量组的方法

$$
 \begin{align*} \boldsymbol{\beta}_1 &= \boldsymbol{\alpha}_1, \\ \boldsymbol{\beta}_2 &= \boldsymbol{\alpha}_2 - \frac{(\boldsymbol{\alpha}_2,\boldsymbol{\beta}_1)}{(\boldsymbol{\beta}_1,\boldsymbol{\beta}_1)}\boldsymbol{\beta}_1, \\ \boldsymbol{\beta}_3 &= \boldsymbol{\alpha}_3 - \frac{(\boldsymbol{\alpha}_3,\boldsymbol{\beta}_1)}{(\boldsymbol{\beta}_1,\boldsymbol{\beta}_1)}\boldsymbol{\beta}_1 - \frac{(\boldsymbol{\alpha}_3,\boldsymbol{\beta}_2)}{(\boldsymbol{\beta}_2,\boldsymbol{\beta}_2)}\boldsymbol{\beta}_2, \\ &\vdots \\ \boldsymbol{\beta}_s &= \boldsymbol{\alpha}_s - \frac{(\boldsymbol{\alpha}_s,\boldsymbol{\beta}_1)}{(\boldsymbol{\beta}_1,\boldsymbol{\beta}_1)}\boldsymbol{\beta}_1 - \frac{(\boldsymbol{\alpha}_s,\boldsymbol{\beta}_2)}{(\boldsymbol{\beta}_2,\boldsymbol{\beta}_2)}\boldsymbol{\beta}_2 - \dots - \frac{(\boldsymbol{\alpha}_s,\boldsymbol{\beta}_{s-1})}{(\boldsymbol{\beta}_{s-1},\boldsymbol{\beta}_{s-1})}\boldsymbol{\beta}_{s-1}. \end{align*} 
$$

这样我们就得到了 **正交向量组** $\boldsymbol{\beta}_1,\boldsymbol{\beta}_2,\dots,\boldsymbol{\beta}_s$，

若再对其单位化，即令

$$
 \boldsymbol{\gamma}_i = \frac{1}{\|\boldsymbol{\beta}_i\|}\boldsymbol{\beta}_i,\ i=1,2,\dots,s, 
$$

则 $\boldsymbol{\gamma}_1,\boldsymbol{\gamma}_2,\dots,\boldsymbol{\gamma}_s$ 是一组与 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\dots,\boldsymbol{\alpha}_s$ 等价的 **标准正交向量组**

> [!note]
> 若 $\boldsymbol{\beta}_i$ 元素为分数，可先整数化，再单位化，不影响最终结果

##### 5）正交矩阵 

###### 定义 

如果方阵 $Q$ 满足 $Q^TQ = QQ^T = E$，则称 $Q$ 为 **正交矩阵**

###### 性质 

$Q$ 为正交矩阵 $\iff (\boldsymbol{\alpha}_i,\boldsymbol{\alpha}_j) = 0,i \neq j$ 且 $(\boldsymbol{\alpha}_i,\boldsymbol{\alpha}_i) = 1$，即正交矩阵等价于其列（或行）向量组为一个标准正交向量组

$Q$ 为正交矩阵 $\implies |Q| = \pm 1$ 且 $\lambda = \pm 1$（注意仅为必要条件）