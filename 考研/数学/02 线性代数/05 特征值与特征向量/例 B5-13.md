---
章: 第五章 特征值与特征向量
节: "[[强化题型总结五]]"
tags:
  - 例题
index: 13
---
> [!question] 
> 设 3 阶矩阵 $\boldsymbol{A}$ 有 3 个不同的特征值 $\lambda_1,\lambda_2,\lambda_3$，对应的特征向量分别为 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3$，令 $\boldsymbol{\beta}=\boldsymbol{\alpha}_1+\boldsymbol{\alpha}_2+\boldsymbol{\alpha}_3$
> 
> （1）求证：$\boldsymbol{\beta},\boldsymbol{A\beta},\boldsymbol{A^2\beta}$ 线性无关
> 
> （2）若 $\boldsymbol{A^3\beta}=\boldsymbol{A\beta}$，求秩 $R(\boldsymbol{A-E})$ 及行列式 $|\boldsymbol{A+2E}|$

（1）由题设 $\boldsymbol{A\alpha}_1=\lambda_1\boldsymbol{\alpha}_1,\boldsymbol{A\alpha}_2=\lambda_2\boldsymbol{\alpha}_2,\boldsymbol{A\alpha}_3=\lambda_3\boldsymbol{\alpha}_3$ 且 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3$ 线性无关，则

$$ \begin{aligned} A\boldsymbol{\beta}&=A(\boldsymbol{\alpha}_1+\boldsymbol{\alpha}_2+\boldsymbol{\alpha}_3)=\lambda_1\boldsymbol{\alpha}_1+\lambda_2\boldsymbol{\alpha}_2+\lambda_3\boldsymbol{\alpha}_3, \\ A^2\boldsymbol{\beta}&=\lambda_1^2\boldsymbol{\alpha}_1+\lambda_2^2\boldsymbol{\alpha}_2+\lambda_3^2\boldsymbol{\alpha}_3\end{aligned} $$

**法一**

$$ \begin{aligned} (\boldsymbol{\beta},A\boldsymbol{\beta},A^2\boldsymbol{\beta})&=(\boldsymbol{\alpha}_1+\boldsymbol{\alpha}_2+\boldsymbol{\alpha}_3,\lambda_1\boldsymbol{\alpha}_1+\lambda_2\boldsymbol{\alpha}_2+\lambda_3\boldsymbol{\alpha}_3,\lambda_1^2\boldsymbol{\alpha}_1+\lambda_2^2\boldsymbol{\alpha}_2+\lambda_3^2\boldsymbol{\alpha}_3) \\ &=(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3)\begin{pmatrix}1&\lambda_1&\lambda_1^2\\1&\lambda_2&\lambda_2^2\\1&\lambda_3&\lambda_3^2\end{pmatrix}\end{aligned} $$

根据[[范德蒙，拉普拉斯公式#2）范德蒙行列式|范德蒙行列式]]，$\begin{vmatrix}1&\lambda_1&\lambda_1^2\\1&\lambda_2&\lambda_2^2\\1&\lambda_3&\lambda_3^2\end{vmatrix}=(\lambda_3-\lambda_2)(\lambda_3-\lambda_1)(\lambda_2-\lambda_1)$

因为 $\lambda_1,\lambda_2,\lambda_3$ 互不相同，所以 $\begin{vmatrix}1&\lambda_1&\lambda_1^2\\1&\lambda_2&\lambda_2^2\\1&\lambda_3&\lambda_3^2\end{vmatrix}\neq0$

根据[[矩阵的逆]]，矩阵 $\begin{pmatrix}1&\lambda_1&\lambda_1^2\\1&\lambda_2&\lambda_2^2\\1&\lambda_3&\lambda_3^2\end{pmatrix}$ 可逆，从而

$$ R(\boldsymbol{\beta},A\boldsymbol{\beta},A^2\boldsymbol{\beta})=R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3)=3$$

故 $\boldsymbol{\beta},A\boldsymbol{\beta},A^2\boldsymbol{\beta}$ 线性无关，证毕

**法二** 定义法

设 $k_1\boldsymbol{\beta}+k_2A\boldsymbol{\beta}+k_3A^2\boldsymbol{\beta}=\boldsymbol{0}$，

将 $\boldsymbol{\beta},A\boldsymbol{\beta},A^2\boldsymbol{\beta}$ 代入，整理得

$$ (k_1+k_2\lambda_1+k_3\lambda_1^2)\boldsymbol{\alpha}_1+(k_1+k_2\lambda_2+k_3\lambda_2^2)\boldsymbol{\alpha}_2+(k_1+k_2\lambda_3+k_3\lambda_3^2)\boldsymbol{\alpha}_3=\boldsymbol{0}$$

又因为 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3$ 分别是 3 个不同特征值对应的特征向量，所以它们必线性无关，从而

$$ \begin{cases} k_1+k_2\lambda_1+k_3\lambda_1^2=0\\k_1+k_2\lambda_2+k_3\lambda_2^2=0\\ k_1+k_2\lambda_3+k_3\lambda_3^2=0\end{cases}$$

其系数行列式 $\begin{vmatrix}1&\lambda_1&\lambda_1^2\\1&\lambda_2&\lambda_2^2\\1&\lambda_3&\lambda_3^2\end{vmatrix}\neq0$，于是方程组只有零解 $k_1=k_2=k_3=0$，即 $\boldsymbol{\beta},A\boldsymbol{\beta},A^2\boldsymbol{\beta}$ 线性无关，证毕

（2）**法一**

由 $A^3\boldsymbol{\beta}=A\boldsymbol{\beta}$ 可知 $\lambda_1^3\boldsymbol{\alpha}_1+\lambda_2^3\boldsymbol{\alpha}_2+\lambda_3^3\boldsymbol{\alpha}_3=\lambda_1\boldsymbol{\alpha}_1+\lambda_2\boldsymbol{\alpha}_2+\lambda_3\boldsymbol{\alpha}_3$，整理可得

$$ (\lambda_1^3-\lambda_1)\boldsymbol{\alpha}_1+(\lambda_2^3-\lambda_2)\boldsymbol{\alpha}_2+(\lambda_3^3-\lambda_3)\boldsymbol{\alpha}_3=\boldsymbol{0}$$

因为 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3$ 线性无关，所以 $\lambda_i^3-\lambda_i=0\ (i=1,2,3)$

又因为 $\lambda_1,\lambda_2,\lambda_3$ 互不相同，得 $\lambda_A$ = -1，0，1

根据[[特征值与特征向量的性质#定理 2|特征值与特征向量的性质]]，$\lambda_{A-E}$ = -2，-1，0，则 $\boldsymbol{A-E}$ 可对角化（特征值互不相同），因而 $R(\boldsymbol{A-E})$ = 2（非零特征值的个数），$\lambda_{A+2E}$ = 1，2，3，故 $|\boldsymbol{A+2E}|=6$

法二

令 $\boldsymbol{P}=(\boldsymbol{\beta},A\boldsymbol{\beta},A^2\boldsymbol{\beta})$，则 $\boldsymbol{P}$ 可逆，按照证明相似的思路

$$ A\boldsymbol{P}=A(\boldsymbol{\beta},A\boldsymbol{\beta},A^2\boldsymbol{\beta})=(A\boldsymbol{\beta},A^2\boldsymbol{\beta},A^3\boldsymbol{\beta})$$

代入 $A^3\boldsymbol{\beta}=A\boldsymbol{\beta}$

$$ \begin{aligned} A\boldsymbol{P}&=(A\boldsymbol{\beta},A^2\boldsymbol{\beta},A\boldsymbol{\beta})=(\boldsymbol{\beta},A\boldsymbol{\beta},A^2\boldsymbol{\beta})\begin{pmatrix}0&0&0\\1&0&1\\0&1&0\end{pmatrix}=\boldsymbol{P}\boldsymbol{B}\end{aligned} $$

故 $\boldsymbol{P}^{-1}A\boldsymbol{P}=\boldsymbol{B}$，$\boldsymbol{A}\sim \boldsymbol{B}$

从而 $\boldsymbol{A-E}\sim \boldsymbol{B-E}$，$\boldsymbol{A+2E}\sim\boldsymbol{B+2E}$，于是

$$
R(\boldsymbol{A-E})=R(\boldsymbol{B-E})=R\begin{pmatrix}-1&0&0\\1&-1&1\\0&1&-1\end{pmatrix}=2
$$

$$
|\boldsymbol{A+2E}|=|\boldsymbol{B+2E}|=\begin{vmatrix}2&0&0\\1&2&1\\0&1&2\end{vmatrix}=6
$$