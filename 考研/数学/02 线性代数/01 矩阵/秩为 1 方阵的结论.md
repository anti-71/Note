---
名称: 秩为 1 方阵的结论
章: 第一章 矩阵
节: "[[强化题型总结 - 1]]"
tags:
  - 知识点
index: 6
---
##### 1）秩为 1 的方阵求 $A^n$

1. $n$ 阶矩阵 $\boldsymbol{A}$ 的秩为 1 $\iff$ 存在 $n$ 维非零列向量 $\boldsymbol{\alpha}$ 和 $\boldsymbol{\beta}$，使 $\boldsymbol{A}=\boldsymbol{\alpha}\boldsymbol{\beta}^\mathrm{T}$
2. $\mathrm{tr}\left(\boldsymbol{\alpha}\boldsymbol{\beta}^\mathrm{T}\right)=\boldsymbol{\beta}^\mathrm{T}\boldsymbol{\alpha}$（$\boldsymbol{\alpha},\boldsymbol{\beta}$ 为 $n$ 维列向量）

> [!success] 证明
> 以 3 维为例，由
> 
> $$ \begin{aligned} l&=\boldsymbol{\alpha}^\mathrm{T}\boldsymbol{\beta}=(a_1,a_2,a_3)\begin{pmatrix}b_1\\b_2\\b_3\end{pmatrix}=a_1b_1+a_2b_2+a_3b_3,\\ \boldsymbol{A}&=\boldsymbol{\alpha}\boldsymbol{\beta}^\mathrm{T}=\begin{pmatrix}a_1\\a_2\\a_3\end{pmatrix}(b_1,b_2,b_3)=\begin{pmatrix}a_1b_1&a_1b_2&a_1b_3\\a_2b_1&a_2b_2&a_2b_3\\a_3b_1&a_3b_2&a_3b_3\end{pmatrix} \end{aligned} $$
> 
> 故 $l=\boldsymbol{\alpha}^\mathrm{T}\boldsymbol{\beta}=\boldsymbol{\beta}^\mathrm{T}\boldsymbol{\alpha}=\mathrm{tr}(\boldsymbol{A})$

> [!note] 
> 由 1、2 知
> 
> $$ \begin{aligned} \boldsymbol{A}^n&=(\boldsymbol{\alpha}\boldsymbol{\beta}^\mathrm{T})^n\\
> &\xlongequal{\text{结合律}}\boldsymbol{\alpha}(\boldsymbol{\beta}^\mathrm{T}\boldsymbol{\alpha})(\boldsymbol{\beta}^\mathrm{T}\boldsymbol{\alpha})\cdots(\boldsymbol{\beta}^\mathrm{T}\boldsymbol{\alpha})\boldsymbol{\beta}^\mathrm{T}\\ 
> &=\boldsymbol{\alpha}(\boldsymbol{\beta}^\mathrm{T}\boldsymbol{\alpha})^{n-1}\boldsymbol{\beta}^\mathrm{T}\\
> &=(\boldsymbol{\beta}^\mathrm{T}\boldsymbol{\alpha})^{n-1}\boldsymbol{\alpha}\boldsymbol{\beta}^\mathrm{T}\\ 
> &=[\mathrm{tr}(\boldsymbol{A})]^{n-1}\boldsymbol{A} \end{aligned} $$

##### 2）秩为 1 的方阵的特征值

若 $n$ 阶方阵 $\boldsymbol{A}=\boldsymbol{\alpha}\boldsymbol{\beta}^\mathrm{T}$ 的秩为 1，则 $\boldsymbol{A}$ 的特征值为

$$ \lambda_1=\mathrm{tr}(\boldsymbol{A}),\lambda_2=\cdots=\lambda_n=0$$

因为 $\boldsymbol{A}\boldsymbol{\alpha}=(\boldsymbol{\alpha}\boldsymbol{\beta}^\mathrm{T})\boldsymbol{\alpha}=\boldsymbol{\alpha}(\boldsymbol{\beta}^\mathrm{T}\boldsymbol{\alpha})=\mathrm{tr}(\boldsymbol{A})\boldsymbol{\alpha}$，所以 $\lambda_1=\mathrm{tr}(\boldsymbol{A})$ 有特征向量为 $\boldsymbol{\alpha}$

##### 3）秩为 1 的方阵的相似对角化判定

若 $n$ 阶方阵 $\boldsymbol{A}=\boldsymbol{\alpha}\boldsymbol{\beta}^\mathrm{T}$ 的秩为 1，则

- 当 $\mathrm{tr}(\boldsymbol{A})\neq 0$ 时，$\boldsymbol{A}$ 可对角化
- 当 $\mathrm{tr}(\boldsymbol{A})=0$ 时，$\boldsymbol{A}$ 不可对角化