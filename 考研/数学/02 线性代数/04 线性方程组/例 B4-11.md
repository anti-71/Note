---
章: 第四章 线性方程组
节: "[[强化题型总结四]]"
tags:
  - 例题
index: 11
---
> [!question] 
> 设 $\boldsymbol{A}=(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4)$ 是 4 阶矩阵，非齐次线性方程组 $\boldsymbol{Ax}=\boldsymbol{\beta}$ 的通解为 $(1,-1,0,1)^\mathrm{T}+k(1,0,-3,2)^\mathrm{T}$，$k$ 为任意常数，记 $\boldsymbol{B}=(\boldsymbol{\alpha}_2+\boldsymbol{\beta},\boldsymbol{\alpha}_4,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_1)$
> 
> （1）求证：$R(\boldsymbol{B})=3$
> 
> （2）求方程组 $\boldsymbol{Bx}=\boldsymbol{\beta}$ 的通解

（1）**证明**：

由 $\boldsymbol{Ax}=\boldsymbol{\beta}$ 的通解为 $(1,-1,0,1)^\mathrm{T}+k(1,0,-3,2)^\mathrm{T}$，根据[[方程组解的性质与结构#6）结论 6|方程组解的性质]]

$$ R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4)=R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4,\boldsymbol{\beta})=3$$

分别代入通解和特解

$$ (\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4)\begin{pmatrix}1\\0\\-3\\2\end{pmatrix}=\boldsymbol{\alpha}_1-3\boldsymbol{\alpha}_3+2\boldsymbol{\alpha}_4=\boldsymbol{0}\tag{1} $$

$$ (\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4)\begin{pmatrix}1\\-1\\0\\1\end{pmatrix}=\boldsymbol{\alpha}_1-\boldsymbol{\alpha}_2+\boldsymbol{\alpha}_4=\boldsymbol{\beta}\tag{2} $$

将 2 式代入矩阵 $\boldsymbol{B}$

$$ \begin{aligned} R(\boldsymbol{B})&=R(\boldsymbol{\alpha}_2+\boldsymbol{\beta},\boldsymbol{\alpha}_4,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_1) \\ &=R(\boldsymbol{\alpha}_2+\boldsymbol{\alpha}_1-\boldsymbol{\alpha}_2+\boldsymbol{\alpha}_4,\boldsymbol{\alpha}_4,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_1) \\ &=R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4)\\
&=3\end{aligned} $$

证毕

 （2）由 $R(\boldsymbol{B})=3$，知 $\boldsymbol{Bx}=\boldsymbol{0}$ 的基础解系中只有两个线性无关的解向量

由 1 式和 2 式，得

$$ (\boldsymbol{\alpha}_2+\boldsymbol{\beta},\boldsymbol{\alpha}_4,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_1)\begin{pmatrix}1\\-1\\0\\0\\-1\end{pmatrix}=\boldsymbol{0}, \quad (\boldsymbol{\alpha}_2+\boldsymbol{\beta},\boldsymbol{\alpha}_4,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_1)\begin{pmatrix}0\\2\\-3\\0\\1\end{pmatrix}=\boldsymbol{0}$$

由 2 式，知 $\boldsymbol{Bx}=\boldsymbol{\beta}$ 的特解为 $(0,1,0,-1,1)^\mathrm{T}$（或由原来的 $B$，知特解为 $(1,0,0,-1,0)^\mathrm{T}$）

故通解为

$$ k_1(1,-1,0,0,-1)^\mathrm{T}+k_2(0,2,-3,0,1)^\mathrm{T}+(0,1,0,-1,1)^\mathrm{T}$$

其中 $k_1,k_2$ 为任意常数