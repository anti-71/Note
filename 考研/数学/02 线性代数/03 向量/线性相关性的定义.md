---
名称: 线性相关性的定义
章: 第三章 向量
节: "[[第三节 向量组的线性相关性]]"
tags:
  - 知识点
index: 1
---
设 $m$ 维向量 $\boldsymbol{\alpha}_1, \boldsymbol{\alpha}_2, \dots, \boldsymbol{\alpha}_s$，如果存在 **不全为零** 的实数 $k_1, k_2, \dots, k_s$，使得

$$
k_1\boldsymbol{\alpha}_1 + k_2\boldsymbol{\alpha}_2 + \dots + k_s\boldsymbol{\alpha}_s = \boldsymbol{0} \tag{3.1}
$$

则称向量组 $\boldsymbol{\alpha}_1, \boldsymbol{\alpha}_2, \dots, \boldsymbol{\alpha}_s$ **线性相关**；否则，称向量组 $\boldsymbol{\alpha}_1, \boldsymbol{\alpha}_2, \dots, \boldsymbol{\alpha}_s$ **线性无关**，即：

当且仅当 $k_1 = k_2 = \dots = k_s = 0$ 时，$k_1\boldsymbol{\alpha}_1 + k_2\boldsymbol{\alpha}_2 + \dots + k_s\boldsymbol{\alpha}_s = \boldsymbol{0}$ 才能成立

> [!note] 
> - **线性无关**：当且仅当 $k_1 = k_2 = \dots = k_s = 0$ 时（系数组合为零组合），等式 $(3.1)$ 才能成立；
> - **线性相关**：系数组合除了零组合以外，还有其他非零组合（系数不全为 0）使得等式 $(3.1)$ 成立