---
章: 第三章 向量
节: "[[强化题型总结三]]"
tags:
  - 例题
index: 15
---
> [!question] 
> 设向量 $\boldsymbol{\beta}$ 可以由向量组 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_s$ 线性表示，但不能被 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1}$ 线性表示，求证：
> 
> （1）$\boldsymbol{\alpha}_s$ 不能被 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1}$ 线性表示；
> 
> （2）$\boldsymbol{\alpha}_s$ 能被 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1},\boldsymbol{\beta}$ 线性表示

**证明**：

**法一**

（1）$\boldsymbol{\beta}$ 可以由向量组 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_s$ 线性表示，即

$$ \boldsymbol{\beta}=k_1\boldsymbol{\alpha}_1+k_2\boldsymbol{\alpha}_2+\cdots+k_{s-1}\boldsymbol{\alpha}_{s-1}+k_s\boldsymbol{\alpha}_s\tag{1} $$

现假设 $\boldsymbol{\alpha}_s$ 能被 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1}$ 线性表示，即 $\boldsymbol{\alpha}_s=l_1\boldsymbol{\alpha}_1+l_2\boldsymbol{\alpha}_2+\cdots+l_{s-1}\boldsymbol{\alpha}_{s-1}$，代入 1 式中有

$$ \boldsymbol{\beta}=(k_1+k_sl_1)\boldsymbol{\alpha}_1+(k_2+k_sl_2)\boldsymbol{\alpha}_2+\cdots+(k_{s-1}+k_sl_{s-1})\boldsymbol{\alpha}_{s-1}$$

即 $\boldsymbol{\beta}$ 能被 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1}$ 线性表示，与题设矛盾，从而假设不成立，即 $\boldsymbol{\alpha}_s$ 不能被 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1}$ 线性表示，证毕

（2）由 1 式有 $\boldsymbol{\beta}=k_1\boldsymbol{\alpha}_1+k_2\boldsymbol{\alpha}_2+\cdots+k_{s-1}\boldsymbol{\alpha}_{s-1}+k_s\boldsymbol{\alpha}_s$，其中定有 $k_s\neq0$（否则 $\boldsymbol{\beta}=k_1\boldsymbol{\alpha}_1+k_2\boldsymbol{\alpha}_2+\cdots+k_{s-1}\boldsymbol{\alpha}_{s-1}$，$\boldsymbol{\beta}$ 可由 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1}$ 线性表示，与题设矛盾）

从而

$$\boldsymbol{\alpha}_s=\frac{1}{k_s}\boldsymbol{\beta}-\frac{k_1}{k_s}\boldsymbol{\alpha}_1-\frac{k_2}{k_s}\boldsymbol{\alpha}_2-\cdots-\frac{k_{s-1}}{k_s}\boldsymbol{\alpha}_{s-1}$$

即 $\boldsymbol{\alpha}_s$ 能被 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1},\boldsymbol{\beta}$ 线性表示，证毕

**法二** 秩

（1）$\boldsymbol{\beta}$ 不能被 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1}$ 线性表示，即

$$R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1}, \boldsymbol{\beta}) = R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1}) + 1\tag{2}$$

$\boldsymbol{\beta}$ 能被 $\boldsymbol{\alpha}_1, \dots, \boldsymbol{\alpha}_{s-1}, \boldsymbol{\alpha}_s$ 线性表示

$$R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_s, \boldsymbol{\beta}) = R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_s)\tag{3}$$

$$
\because R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_s, \boldsymbol{\beta})\geqslant R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1}, \boldsymbol{\beta})
$$

代入 2、3 式

$$
R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_s)\geqslant R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1}) + 1
$$

$$
\begin{aligned}
又&\because R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_s)\leqslant R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1}) + 1\\\\
&\therefore R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_s)=R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1}) + 1
\end{aligned}
$$

即 $\boldsymbol{\alpha}_s$ 不能被 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1}$ 线性表示，证毕

（2）由（1）和 2、3 式可得

$$
R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1}, \boldsymbol{\beta}) = R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_s, \boldsymbol{\beta})
$$

即 $\boldsymbol{\alpha}_s$ 能被 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\cdots,\boldsymbol{\alpha}_{s-1},\boldsymbol{\beta}$ 线性表示，证毕