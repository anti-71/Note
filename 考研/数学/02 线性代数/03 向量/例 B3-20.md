---
章: 第三章 向量
节: "[[强化题型总结三]]"
tags:
  - 例题
index: 20
---
> [!question] 
> 设向量 $\boldsymbol{r}$ 在 $\mathbf{R}^3$ 的基 $\boldsymbol{\alpha}_1=(1,0,1)^\mathrm{T},\boldsymbol{\alpha}_2=(0,2,1)^\mathrm{T},\boldsymbol{\alpha}_3=(0,1,1)^\mathrm{T}$ 下的坐标为 $(x_1,x_2,x_3)^\mathrm{T}$，$(y_1,y_2,y_3)^\mathrm{T}$ 是 $\boldsymbol{r}$ 在 $\mathbf{R}^3$ 的基 $\boldsymbol{\beta}_1,\boldsymbol{\beta}_2,\boldsymbol{\beta}_3$ 下的坐标，且 $y_1=x_1,y_2=x_2-x_1,y_3=x_3-x_2$，求基 $\boldsymbol{\beta}_1,\boldsymbol{\beta}_2,\boldsymbol{\beta}_3$

设由基 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3$ 到基 $\boldsymbol{\beta}_1,\boldsymbol{\beta}_2,\boldsymbol{\beta}_3$ 的[[过渡矩阵]]为 $\boldsymbol{C}$，即

$$ (\boldsymbol{\beta}_1,\boldsymbol{\beta}_2,\boldsymbol{\beta}_3)=(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3)\boldsymbol{C}\tag{1} $$

依题设可知 $\boldsymbol{r}=(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3)\begin{pmatrix}x_1\\x_2\\x_3\end{pmatrix}=(\boldsymbol{\beta}_1,\boldsymbol{\beta}_2,\boldsymbol{\beta}_3)\begin{pmatrix}y_1\\y_2\\y_3\end{pmatrix}$，

$$(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3)\begin{pmatrix}x_1\\x_2\\x_3\end{pmatrix}=(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3)\boldsymbol{C}\begin{pmatrix}y_1\\y_2\\y_3\end{pmatrix}$$

即 $\begin{pmatrix}x_1\\x_2\\x_3\end{pmatrix}=\boldsymbol{C}\begin{pmatrix}y_1\\y_2\\y_3\end{pmatrix}$

已知 $y_1=x_1,y_2=x_2-x_1,y_3=x_3-x_2$，即

$$\begin{cases} x_1 = y_1 \\ x_2 = y_1 + y_2 \\ x_3 = y_1 + y_2 + y_3 \end{cases}$$

从而 $\boldsymbol{C}=\begin{pmatrix}1&0&0\\1&1&0\\1&1&1\end{pmatrix}$

代入 1 式有 $(\boldsymbol{\beta}_1,\boldsymbol{\beta}_2,\boldsymbol{\beta}_3)=(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3)\begin{pmatrix}1&0&0\\1&1&0\\1&1&1\end{pmatrix}=(\boldsymbol{\alpha}_1+\boldsymbol{\alpha}_2+\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_2+\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_3)$，从而

$$ \boldsymbol{\beta}_1=\boldsymbol{\alpha}_1+\boldsymbol{\alpha}_2+\boldsymbol{\alpha}_3=(1,3,3)^\mathrm{T},\ \boldsymbol{\beta}_2=\boldsymbol{\alpha}_2+\boldsymbol{\alpha}_3=(0,3,2)^\mathrm{T},\ \boldsymbol{\beta}_3=\boldsymbol{\alpha}_3=(0,1,1)^\mathrm{T}$$