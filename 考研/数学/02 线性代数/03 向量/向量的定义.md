---
名称: 向量的定义
章: 第三章 向量
节: "[[第一节 向量的定义与运算]]"
tags:
  - 知识点
index: 1
---
$n$ 个数 $a_1,a_2,\dots,a_n$ 所组成的有序数组 

$$
\boldsymbol{\alpha} = (a_1,a_2,\dots,a_n) \quad \text{或} \quad \boldsymbol{\alpha} = (a_1,a_2,\dots,a_n)^T
$$

称为 $n$ **维向量**

其中 $a_1,a_2,\dots,a_n$ 称为向量 $\boldsymbol{\alpha}$ 的 **分量**（或 **坐标**）；前一个表示式为 **行向量**，后者称为 **列向量**

向量是只包含一行或一列元素的矩阵