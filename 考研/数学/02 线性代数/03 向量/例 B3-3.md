---
章: 第三章 向量
节: "[[强化题型总结三]]"
tags:
  - 例题
index: 3
---
> [!question] 
> 设有 $n$ 维向量 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4$，下列命题中正确的是（）
> 
> - ① 若 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3$ 线性无关，$\boldsymbol{\alpha}_4$ 不能由 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3$ 线性表示，则 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4$ 线性无关
> - ② 若存在 $n$ 阶矩阵 $\boldsymbol{A}$，使得 $A\boldsymbol{\alpha}_1,A\boldsymbol{\alpha}_2,A\boldsymbol{\alpha}_3,A\boldsymbol{\alpha}_4$ 线性无关，则 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4$ 线性无关
> - ③ 若 $A\boldsymbol{\beta}_1=\boldsymbol{\alpha}_1,A\boldsymbol{\beta}_2=\boldsymbol{\alpha}_2,A\boldsymbol{\beta}_3=\boldsymbol{\alpha}_3,A\boldsymbol{\beta}_4=\boldsymbol{\alpha}_4$，且 $A$ 可逆，$\boldsymbol{\beta}_1,\boldsymbol{\beta}_2,\boldsymbol{\beta}_3,\boldsymbol{\beta}_4$ 线性无关，则 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4$ 线性无关
> - ④ 若 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2$ 线性无关，$\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4$ 均不能由 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2$ 线性表示，则 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4$ 线性无关
> 
> （A）①②③
> 
> （B）①②④
> 
> （C）②③④
> 
> （D）①③④

① 显然正确

因为 $(A\boldsymbol{\alpha}_1,A\boldsymbol{\alpha}_2,A\boldsymbol{\alpha}_3,A\boldsymbol{\alpha}_4)=A(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4)$，所以

$$ R(A\boldsymbol{\alpha}_1,A\boldsymbol{\alpha}_2,A\boldsymbol{\alpha}_3,A\boldsymbol{\alpha}_4)\leqslant R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4)\leqslant 4$$

而 $R(A\boldsymbol{\alpha}_1,A\boldsymbol{\alpha}_2,A\boldsymbol{\alpha}_3,A\boldsymbol{\alpha}_4)=4$，故 $R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4)=4$，即 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4$ 线性无关，② 正确

当 $A$ 可逆时，$R(\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4)=R\left[A(\boldsymbol{\beta}_1,\boldsymbol{\beta}_2,\boldsymbol{\beta}_3,\boldsymbol{\beta}_4)\right]=R(\boldsymbol{\beta}_1,\boldsymbol{\beta}_2,\boldsymbol{\beta}_3,\boldsymbol{\beta}_4)=4$，所以 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4$ 线性无关，③ 正确

在该条件下，$\alpha_3=\alpha_4$，则 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3,\boldsymbol{\alpha}_4$ 线性相关，④ 不正确

综上所述，故应选（A）