---
名称: 客户 ｜ 服务器模型
章: 第六章 应用层
节: "[[第一节 网络应用模型]]"
tags:
  - 知识点
index: 1
---
##### 客户 / 服务器（C/S）模型概述

在 **客户 / 服务器**（C/S）模型中，存在一个始终处于开启状态的主机，称为 **服务器**，它负责响应来自多个称为 **客户** 的主机的服务请求，如下图所示

![image.png|436x258](https://shiyuzhe.oss-cn-hangzhou.aliyuncs.com/Tuchuang/20260611202705830.png)

##### 客户 / 服务器模型的工作流程

1. 服务器持续处于接收请求的状态
2. 客户主动发起服务请求，并等待接收处理结果
3. 服务器收到请求后，解析并处理该请求，将结果返回给客户

服务器上运行着专门提供某种服务的程序，能够同时处理多个来自远程或本地客户程序的请求

客户程序必须事先知道服务器程序的地址；而服务器程序启动后便 **一直不断运行** 着，被动等待并接收来自各地客户程序的请求

因此，服务器程序无须知道客户程序的地址

##### 客户 / 服务器模型的主要特征

**客户是服务请求方，服务器是服务提供方**

> [!example] 
> 在 Web 应用中，始终运行的 Web 服务器响应运行在客户上的浏览器所发出的请求
> 
> 当 Web 服务器收到来自客户对某一对象的请求时，会将该对象发送回客户作为响应

##### 采用 C/S 模型的常见应用

Web、文件传输协议（FTP）和电子邮件（SMTP/POP3/IMAP）

##### 客户 / 服务器模型的其他特点

###### 1）网络中各计算机的地位不平等

服务器可通过用户权限控制来管理客户，整个网络的管理工作由少数服务器集中承担，因此管理更加集中和便捷

###### 2）客户之间通常不直接通信

例如，在 Web 应用中，两个浏览器不会直接交换数据

###### 3）可扩展性不佳

受服务器性能和网络带宽的制约，单个服务器支持的客户数量有限