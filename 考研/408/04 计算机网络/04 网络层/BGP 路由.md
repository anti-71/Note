---
名称: BGP 路由
章: 第四章 网络层
节: "[[第四节 路由算法与路由协议]]"
tags:
  - 知识点
index: 13
---
##### BGP 路由的一般格式

$$\text{BGP 路由} = <前缀, \text{BGP 属性}> = <前缀, \text{AS-PATH}, \text{NEXT-HOP}>$$

BGP 属性有多种类型

当一个路由器通过 BGP 会话向对等方通告一条 BGP 路由时，最重要的两个属性是 AS-PATH（**自治系统路径**）和 NEXT-HOP（**下一跳**）

###### AS-PATH 属性说明

**AS-PATH** 记录了该 BGP 路由所经过的自治系统序列

在 BGP 中，每个自治系统由一个全局唯一的自治系统号（ASN）标识

BGP 路由每经过一个 AS，就向其 ASN 加入 AS-PATH

可见，BGP 路由明确指出了 **所经过的 AS 路径**，但不指明具体经过哪些路由器

###### NEXT-HOP 属性说明

**NEXT-HOP** 表示接收该 BGP 路由的路由器去往目的网络时应使用的下一跳地址

具体而言，从本 AS 出发时，需将分组发给 NEXT-HOP，其通常是通告此路由的邻居 AS 的边界路由器上与本 AS 直连的接口 IP 地址，即 AS-PATH 中第一个 AS（邻居 AS）对应的入口点

##### AS 内路由器分类

在一个 AS 中有两类功能不同的路由器：

- **边界路由器**
- **内部路由器**

##### BGP 会话相关概念

在这些边界路由器之间，以及在 AS 内部的 BGP 路由器之间，均通过端口号为 179 的 **半永久 TCP** 连接（交换信息后仍保持连接）来交换 BGP 路由信息

每对通过 TCP 连接交换 BGP 报文的路由器称为 **BGP 对等方**，该 TCP 连接称为 **BGP 会话**

其中，跨越两个 AS 的 BGP 会话称为 **外部 BGP**（eBGP）会话，而位于同一 AS 内的 BGP 会话称为 **内部 BGP**（iBGP）会话

可见，BGP 不仅用于 AS 之间的路由交换，还运行于 AS 内部，以确保外部路由信息在整个 AS 中正确传播

为避免路由信息丢失，BGP 要求在 AS 内的 **任意两台路由器之间都要建立 iBGP 会话**，但不要求物理直连（如下图中 R2 与 R4、R2 与 R5 之间的 iBGP 会话）

##### BGP 路由解析实例

在下图中，位于 AS2 的 R4 收到一条 **BGP 路由** “X, AS-PATH = AS1, NEXT-HOP = R1”，表示 “从 R1 出发，经过 AS1，可到达网络 X”，即 “R1 → X”

![image.png|603x146](https://shiyuzhe.oss-cn-hangzhou.aliyuncs.com/Tuchuang/20260607214602458.png)

R4 为了构造自己的路由表，需要解析该路由的 NEXT-HOP

由于 R1 位于 AS1，AS2 的内部路由器无法直接将分组发往 R1，因此 R4 必须通过 AS2 中的某个边界路由器来抵达 NEXT-HOP

具体地，R4 知道 R1 是其 eBGP 对等方 R2 的邻居，因此通往 R1 的流量必须先发给 R2，形成的路径为 “R2 → R1 → X”

由于 R2 在 AS2 中，AS2 内的所有路由器都能将分组转发到 R2，再经过 R1，最终到达 X

接着，R4 利用内部网关协议（IGP），找到从自身到 R2 的最优路径，假设查得下一跳为 R3

于是，R4 在路由表中添加表项 <X, R3>

此后，R4 收到目的网络为 X 的分组时，将按路径 R4 → R3 → R2 → R1 → X 转发

类似地，R3 也会在其路由表中添加表项 <X, R2>

每个路由器在收到新的 BGP 路由通告后，都需要执行 **NEXT-HOP 解析** 和 **IGP 查找**，才能将路由正确写入本地路由表