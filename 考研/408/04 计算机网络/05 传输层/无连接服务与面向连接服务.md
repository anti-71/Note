---
名称: 无连接服务与面向连接服务
章: 第五章 传输层
节: "[[第一节 传输层提供的服务]]"
tags:
  - 知识点
index: 3
---
##### 两大传输层协议概述

TCP/IP 协议族在 IP 层之上定义了两个主要的传输协议：

- **TCP**（**传输控制协议**）：面向连接，提供可靠的全双工逻辑信道
- **UDP**（**用户数据报协议**）：无连接，提供不可靠的逻辑信道

##### TCP 协议特点与适用场景

TCP 要求通信双方在数据传输前先建立连接，传输结束后释放连接

TCP 不提供广播或多播服务

TCP 通过确认、流量控制、计时器和连接管理等机制保障可靠传输，但代价是首部开销大、处理资源消耗高

因此，TCP 适用于对可靠性要求高的场景，如 FTP、HTTP、SMTP 等

##### UDP 协议特点与适用场景

UDP 无须建立连接，接收方收到数据报后，也不发送确认

它在 IP 层之上仅提供两项服务：

- 多路复用与分用
- 数据差错检测

由于结构简单、开销小，UDP 执行效率高、实时性好，适用于对时延敏感、可容忍少量丢包的应用，如 TFTP、DNS、DHCP 和 RIP 等

##### 典型应用与对应传输协议

|互联网应用|TCP/IP 应用层协议|TCP/IP 传输层协议|
|---|---|---|
|域名解析|域名系统（DNS）|UDP|
|文件传送|简单文件传送协议（TFTP）|UDP|
|路由选择|路由信息协议（RIP）|UDP|
|IP 地址分配|动态主机配置协议（DHCP）|UDP|
|网络管理|简单网络管理协议（SNMP）|UDP|
|电子邮件|简单邮件传送协议（SMTP）|TCP|
|远程终端接入|远程终端协议（TELNET）|TCP|
|万维网|超文本传送协议（HTTP）|TCP|
|文件传送|文件传送协议（FTP）|TCP|

> [!warning] IP 数据报和 UDP 数据报的区别
> 
> - IP 数据报在网络层需经路由器存储转发
> - UDP 数据报作为 IP 数据报的载荷，在网络层传输时，其内容对路由器不可见