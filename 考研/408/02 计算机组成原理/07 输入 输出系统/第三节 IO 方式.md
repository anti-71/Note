---
名称: 第三节 IO 方式
章: 第七章 输入/输出系统
tags:
  - 章节
index: 3
---
输入 / 输出系统实现主机与 I/O 设备之间的数据传送，可采用不同的控制方式

各种方式在硬件代价、系统性能及适用场景等方面各有侧重

常用的 I/O 方式包括程序查询、程序中断和 DMA 等，其中前两种方式高度依赖 **CPU 执行程序指令** 来完成控制

### 一、程序查询方式

- #### [[程序查询方式]]

### 二、程序中断方式

- #### [[程序中断的基本概念|1. 程序中断的基本概念]]
- #### [[程序中断的工作流程|2. 程序中断的工作流程]]
- #### [[多重中断和中断屏蔽技术|3. 多重中断和中断屏蔽技术]]

### 三、DMA 方式

**DMA 方式** 是一种 **完全由硬件控制** 的数据传输方法，它具有程序中断方式的优点，即在数据准备阶段，CPU 与外设可并行工作

DMA 在外设与内存之间建立了一条 **直接的数据通路**，使得信息传送无须经过 CPU，从而显著降低 CPU 在数据传输过程中的负担

正因如此，该方式被称为 **直接存储器存取**，并避免了保存与恢复 CPU 现场等复杂操作

DMA 特别适用于磁盘、显卡、声卡、网卡等高速设备的大批量数据传输，但其硬件实现开销较高

在 DMA 方式中，中断的作用仅限于 **处理故障** 和 **正常传输完成的通知**

- #### [[DMA 方式的特点|1. DMA 方式的特点]]
- #### [[DMA 控制器的组成|2. DMA 控制器的组成]]
- #### [[DMA 的传送方式|3. DMA 的传送方式]]
- #### [[DMA 的传送过程|4. DMA 的传送过程]]
- #### [[DMA 方式和中断方式的区别|5. DMA 方式和中断方式的区别]]