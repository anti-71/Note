---
名称: 页表
章: 第三章 存储系统
节: "[[第六节 虚拟存储器]]"
tags:
  - 知识点
index: 2
---
下图是一个页表示例

![image.png|761x266](https://shiyuzhe.oss-cn-hangzhou.aliyuncs.com/Tuchuang/20260417204211155.png)

##### 页表项基本字段

###### 1）有效位

也称 **装入位**，表示对应虚页是否已调入主存

- 若为 1，表示该页已在主存，页表项中存放其物理页号
- 若为 0，表示未调入，页表项通常存放该页在外存（如磁盘）中的地址

###### 2）脏位

也称 **修改位**，表示页面是否被修改过

在采用回写策略的虚拟存储系统中，置换页面时根据脏位决定是否需将其写回磁盘

###### 3）引用位

也称 **使用位**，记录页面是否被访问过，主要用于实现基于使用历史的页面替换算法（如 Clock 或 LRU 算法）

> [!example] 以上图的页表为例
> - 若 CPU 访问第 1 页，有效位为 1，说明该页已驻留主存
> 
> 地址转换部件将虚拟地址转换为物理地址，CPU 即可访问对应的物理页中的数据
> 
> - 若访问第 5 页，有效位为 0，则发生 **缺页异常**，系统调用缺页处理程序
> 
> 该程序根据页表项中的外存地址，将该页从磁盘调入一个空闲的物理页框
> 
> - 若主存已满，则需选择一个页面进行置换；由于系统采用回写策略，换出页面时根据脏位决定是否写回磁盘
> 
> 缺页处理完成后，更新页表中的相应项

##### 页式虚拟存储器的优点

页面大小固定，页表结构简单，调入操作方便

##### 页式虚拟存储器的缺点

程序大小通常不是页长的整数倍，导致 **最后一页产生内部碎片**

此外，页是物理划分单位，**缺乏逻辑意义**，因此在程序模块化、保护和共享方面不如段式虚拟存储器灵活