---
名称: 第五节 高速缓冲存储器
章: 第三章 存储系统
tags:
  - 章节
index: 5
---
程序的转移概率通常较高，数据分布也较为离散，因此单纯依赖并行主存系统来提升主存效率是有限的

**高速缓存**（Cache）具有比主存更快的访问速度，因此在 CPU 与主存之间设置 Cache 可以显著提升存储系统的整体效率

Cache 由 SRAM 组成，通常集成在 CPU 内部

### 一、程序访问的局部性原理

- #### [[程序访问的局部性原理]]

### 二、Cache 的基本工作原理

为便于 Cache 与主存交换信息，Cache 和主存都被划分为 **大小相等的块**，**Cache 块** 也称 **Cache 行**，每块由若干字节组成，块的长度称为 **块长**（也称 **行长**）

因为 Cache 的容量远小于主存的容量，所以 Cache 中的块数要远少于主存中的块数，Cache 中仅保存主存中最活跃的若干块的副本

因此，可按照某种策略预测 CPU 在未来一段时间内待访存的数据，将其装入 Cache

- #### [[Cache 的访问过程|1. Cache 的访问过程]]
- #### [[Cache 的命中率分析|2. Cache 的命中率分析]]

### 三、Cache 和主存的映射方式

由于 Cache 行数远少于主存块数，Cache 只能存放主存中部分块的副本

为识别每个 Cache 行对应哪个主存块，需要为每行设置一个 **标记位**，**记录其主存块编号**

同时设置一位 **有效位**，用于 **指示该行数据是否有效**

系统启动或复位时，所有 Cache 行均无效；仅当主存块被装入某 Cache 行后，其有效位才置为 1

**地址映射** 是指将主存地址空间按一定规则映射到 Cache 地址空间，即决定主存块如何装入 Cache

常见的映射方式有三种，包括 **直接映射**、**组相联映射** 和 **全相联映射**

- #### [[直接映射|1. 直接映射]]
- #### [[全相联映射|2. 全相联映射]]
- #### [[组相联映射|3. 组相联映射]]

在 Cache 容量和主存块大小固定的条件下，三种映射方式的特性对比如下：

1. **命中率**：直接映射最低，全相联映射最高
2. **判断开销与所需时间**：直接映射最小、最快，全相联映射最大、最慢
3. **标记存储开销**：直接映射最少，全相联映射最多

### 四、Cache 中主存块的替换算法

- #### [[Cache 中主存块的替换算法]]

### 五、Cache 的一致性问题

由于 Cache 中的内容是主存块的副本，当对 Cache 进行写操作时，必须采用适当的写策略以维持 Cache 与主存数据的一致性

根据写操作是否命中 Cache，可分为两类情况：

- 所谓 **写命中** 是指 CPU 要写入的主存地址所在的块当前已在 Cache 中
- 反之则为 **写不命中**

- #### [[Cache 写命中的处理方法|1. Cache 写命中的处理方法]]
- #### [[Cache 写不命中的处理方法|2. Cache 写不命中的处理方法]]

### 六、Cache 容量的计算举例

- #### [[Cache 容量的计算举例]]

### 七、Cache 的应用

- #### [[Cache 的应用]]