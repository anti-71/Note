---
名称: 按信息的可更改性分类
章: 第三章 存储系统
节: "[[第一节 存储器概览]]"
tags:
  - 知识点
index: 3
---
按信息的可更改性，可分为 **可读可写存储器** 和 **只读存储器**（ROM）

##### 只读存储器（ROM）

**ROM** 中的信息在正常工作时只能读取，通常不可修改

但某些类型（如 E²PROM、Flash）支持特定条件下的重写

##### 可读可写存储器

**RAM** 属于可读可写存储器，与 ROM 一样，通常采用 **随机存取** 方式