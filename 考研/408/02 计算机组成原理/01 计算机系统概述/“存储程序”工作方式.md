---
名称: “存储程序”工作方式
章: 第一章 计算机系统概述
节: "[[第二节 计算机系统层次结构]]"
tags:
  - 知识点
index: 11
---
##### “存储程序” 工作方式规定

在程序执行前，需将其包含的指令和数据预先加载到 **主存储器** 中；

一旦启动，计算机便 **无须人工干预**，自动逐条取出并执行指令

##### 程序的执行过程

每条指令的执行通常包括以下步骤：

![image.png|325x313](https://shiyuzhe.oss-cn-hangzhou.aliyuncs.com/Tuchuang/20260407192847138.png)

从主存储器中取指令（地址由 **程序计数器 PC** 提供）、对指令译码、取操作数、执行操作，并将结果写回存储器

##### 指令流连续执行机制

程序开始执行前，先将第一条指令的地址存入 PC

取指令时，CPU 使用 PC 的内容作为地址访问主存储器

在每条指令执行的最后阶段，系统根据指令类型更新 PC：

- 若为 **顺序指令**，则下一条指令地址为当前 PC 值加上指令长度
- 若为 **跳转指令**，则下一条指令地址为指令中指定的目标地址

随后，CPU 根据更新后的 PC 从主存储器中取出下一条待执行的指令，从而实现指令流的自动连续执行