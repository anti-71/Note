---
名称: 第三节 浮点数的表示与运算
章: 第二章 数据的表示和运算
tags:
  - 章节
index: 3
---
浮点数表示法通过将比例因子嵌入数据中，使小数点位置可根据需要浮动

这样，在有限位数下，既能扩大数值的表示范围，又能保持较高的有效精度

> [!example] 
> 用定点数表示电子质量（$9 \times 10^{-28}\text{g}$）或太阳质量（$2 \times 10^{33}\text{g}$）极为不便

而浮点数则能高效处理此类极大或极小的数值

通常，浮点数表示为

$$N=(-1)^S×M×R^E$$

其中：

- S（取值 0 或 1）决定浮点数的 **符号**；
- M 是一个非负的定点小数，称为 **尾数**，通常用 **原码** 表示；
- E 是一个定点整数，称为 **阶码**（或 **指数**），通常采用 **偏置** 表示（一种移码形式）；
- R 是 **基数**（通常隐含约定为 2、4 或 16）

可见，浮点数由 **符号**、**尾数** 和 **阶码** 三部分组成

在 IEEE 754 浮点数标准广泛使用之前，不同计算机所用的浮点数表示格式各不相同

> [!example] 一种典型的 32 位短浮点数格式示例
> ![image.png|516x88](https://shiyuzhe.oss-cn-hangzhou.aliyuncs.com/Tuchuang/20260411205541142.png)
> 
> 其中：
> 
> - 第 0 位为符号 S；
> - 第 1～7 位为阶码 E，采用偏置值为 64 的移码表示；
> - 第 8～31 位为 24 位尾数 M，以二进制原码小数表示；
> - 基数 R 为 2
> 
> 在该格式中，阶码的值决定了小数点的实际位置；阶码的位数决定了浮点数的表示范围；尾数的位数则决定了数值的精度

### 一、IEEE 754 标准的浮点数

- #### [[IEEE 754 标准的浮点数格式|1. IEEE 754 标准的浮点数格式]]
- #### [[IEEE 754 格式浮点数的表示范围|2. IEEE 754 格式浮点数的表示范围]]
- #### [[几种特殊的 IEEE 754 浮点数|3. 几种特殊的 IEEE 754 浮点数]]

### 二、浮点数的加减运算

浮点数运算的特点是阶码与尾数分开处理，浮点数加减运算分为以下几个步骤

- #### [[对阶|1. 对阶]]
- #### [[尾数加减|2. 尾数加减]]
- #### [[尾数规格化|3. 尾数规格化]]
- #### [[舍入|4. 舍入]]
- #### [[溢出判断|5. 溢出判断]]

### 三、C 语言中的浮点数类型

- #### [[C 语言中的浮点数类型]]

### 四、数据的宽度和存储

- #### [[数据的宽度和单位|1. 数据的宽度和单位]]
- #### [[数据的 “大端方式” 和 “小端方式” 存储|2. 数据的 “大端方式” 和 “小端方式” 存储]]
- #### [[数据按 “边界对齐” 方式存储|3. 数据按 “边界对齐” 方式存储]]



