---
名称: 连接两个循环单链表
章: 代码题
节: "[[第二节 链表]]"
tags:
  - 知识点
index: 12
---
##### 问题描述

> [!question] 
> 有两个循环单链表，链表头指针分别为 $h_1$ 和 $h_2$，编写一个函数将链表 $h_2$ 链接到链表 $h_1$ 之后，要求链接后的链表仍保持循环链表形式

##### 算法思想

先找到 h1 的尾结点，将它的 next 指向 h2 的第一个数据结点，再找到 h2 的尾结点，将它的 next 指回 h1 的头结点，使整条链恢复循环

##### 代码实现

```cpp
void Connect(CLinkList &H1, CLinkList &H2) {
    LNode *p = H1.head;
    while (p->next != H1.head)   // 寻找 H1 的尾结点
        p = p->next;
    p->next = H2.head->next;     // 将 H1 尾结点接到 H2 的第一个结点
    while (p->next != H2.head)   // 顺着找 H2 的尾结点
        p = p->next;
    p->next = H1.head;           // 让 H2 尾结点指回头结点，恢复循环
}
```

##### 复杂度分析

- **时间复杂度：** $O(n + m)$ — 各遍历一次两个链表
- **空间复杂度：** $O(1)$ — 仅使用常数个辅助变量

##### 参考答案

```cpp
LinkList Link(LinkList &h1, LinkList &h2) {
    // 将循环链表 h2 链接到循环链表 h1 之后，使之仍保持循环链表的形式
    LNode *p, *q;                // 分别指向两个链表的尾结点
    p = h1;
    while (p->next != h1)        // 寻找 h1 的尾结点
        p = p->next;
    q = h2;
    while (q->next != h2)        // 寻找 h2 的尾结点
        q = q->next;
    p->next = h2;                // 将 h2 链接到 h1 之后
    q->next = h1;                // 令 h2 的尾结点指向 h1
    return h1;
}
```

##### 补充说明

你的代码与参考答案思路等价：参考答案用 p、q 两个指针分别找两个链表的尾结点，你的代码先用一个 while 找 H1 尾结点并接上 H2，再顺着新链走完 H2 找到其尾结点，效果相同

注意 H2 为空表时，第二个 while 不进入，直接 `p->next = H1.head` 即可，你的写法也能正确处理

##### 评分分析

**评分：10 / 10**

| 方面 | 得分 | 说明 |
|------|:----:|------|
| 算法正确性 | ✅ 满分 | 正确完成链接并保持循环结构 |
| 边界处理 | ✅ 满分 | H1 或 H2 为空表时均能正确处理 |
| 复杂度 | ✅ 满分 | $O(n + m)$ 时间，$O(1)$ 空间 |

##### 修改建议

无，代码正确且简洁
