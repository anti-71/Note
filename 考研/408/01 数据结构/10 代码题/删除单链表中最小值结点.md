---
名称: 删除单链表中最小值结点
章: 代码题
节: "[[第二节 链表]]"
tags:
  - 知识点
index: 2
---
##### 问题描述

> [!question] 
> 试编写在带头结点的单链表 L 中删除一个最小值结点的高效算法（假设该结点唯一）

##### 算法思想

用指针 p 从头结点开始遍历，每次检查 p->next，同时维护最小值结点指针 min 及其前驱指针 min_pre。若当前结点的数据小于 min 的数据，则更新 min 和 min_pre。遍历结束后，通过 min_pre 将 min 结点从链表中删除并释放空间

##### 代码实现

```cpp
void Delete_Min(LinkList &L) {
    LNode *p = L.head;
    LNode *min = p->next;
    LNode *min_pre = p;
    while (p->next != NULL) {
        if (p->next->data < min->data) {
            min_pre = p;
            min = p->next;
        }
        p = p->next;
    }
    min_pre->next = min->next;
    free(min);
}
```

##### 复杂度分析

- **时间复杂度：** $O(n)$ — 只需一次扫描
- **空间复杂度：** $O(1)$ — 仅使用常数个辅助变量

##### 参考答案

```cpp
LinkList Delete_Min(LinkList &L) {
    LNode *pre = L, *p = pre->next;      // p 为工作指针，pre 指向其前驱
    LNode *minpre = pre, *minp = p;      // 保存最小值结点及其前驱
    while (p != NULL) {
        if (p->data < minp->data) {       // 找到更小的结点
            minp = p;
            minpre = pre;
        }
        pre = p;                          // 继续扫描下一个结点
        p = p->next;
    }
    minpre->next = minp->next;            // 删除最小值结点
    free(minp);
    return L;
}
```

##### 补充说明

你的解法通过检查 p->next 来遍历，与"删除所有值为 x 的结点"采用相同思路，删除时 p 不移动即可自然保持在最小值结点的前驱位置，省去了参考答案中额外维护 pre 的步骤

##### 评分分析

**评分：10 / 10**

| 方面 | 得分 | 说明 |
|------|:----:|------|
| 算法正确性 | ✅ 满分 | 能正确找到并删除最小值结点，释放空间 |
| 边界处理 | ✅ 满分 | 最小值为首结点、尾结点、中间结点均正确 |
| 代码简洁 | ✅ 满分 | 单指针 p->next 遍历 + 双变量 min/min_pre，简洁高效 |
| 复杂度 | ✅ 满分 | $O(n)$ 时间，$O(1)$ 空间 |

##### 修改建议

无，代码已正确且简洁
