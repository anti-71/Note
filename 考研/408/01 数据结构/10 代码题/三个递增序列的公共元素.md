---
名称: 三个递增序列的公共元素
章: 代码题
节: "[[第一节 线性表]]"
tags:
  - 知识点
index: 9
---
##### 问题描述

> [!question] 
> 给定三个序列 A、B、C，长度均为 n，且均为无重复元素的递增序列，请设计一个时间上尽可能高效的算法，逐行输出同时存在于这三个序列中的所有元素
>
> 例如，数组 A 为 $\{1,2,3\}$，数组 B 为 $\{2,3,4\}$，数组 C 为 $\{-1,0,2\}$，则输出 2
>
> 要求：
> 1. 给出算法的基本设计思想
> 2. 根据设计思想，采用 C 或 C++ 语言描述算法，关键之处给出注释
> 3. 说明你的算法的时间复杂度和空间复杂度

##### 算法思想

定义三个指针并初始化，每个指针顺序指向数组，进行循环，条件为三个指针都小于 n，循环内判断，如果三个指针指向的元素值相等，则输出，否则，哪个指针指向的元素最小，那个指针增加一个单位

##### 代码实现

```cpp
void Print_Same(SqList &A, SqList &B, SqList &C) {
    int i = 0, j = 0, k = 0, n = A.length;
    while (i < n && j < n && k < n) {
        if (A.elem[i] == B.elem[j] && A.elem[i] == C.elem[k]) {
            printf("%d\n", A.elem[i]);
            i++;
            j++;
            k++;
        }
        else if (A.elem[i] <= B.elem[j] && A.elem[i] <= C.elem[k])
            i++;
        else if (B.elem[j] <= A.elem[i] && B.elem[j] <= C.elem[k])
            j++;
        else
            k++;
    }
}
```

##### 复杂度分析

- **时间复杂度：** $O(n)$
- **空间复杂度：** $O(1)$

##### 参考答案

```cpp
void samekey(int A[], int B[], int C[], int n) {
    int i = 0, j = 0, k = 0;                  // 定义三个工作指针
    while (i < n && j < n && k < n) {
        if (A[i] == B[j] && B[j] == C[k]) {   // 相同则输出，并集体后移
            printf("%d\n", A[i]);
            i++; j++; k++;
        } else {
            int maxNum = max(A[i], max(B[j], C[k]));
            if (A[i] < maxNum) i++;
            if (B[j] < maxNum) j++;
            if (C[k] < maxNum) k++;
        }
    }
}
```

##### 补充说明

本题与"合并两个有序顺序表"思路相似，核心都是利用有序性通过多指针线性扫描

此处扩展为三个序列，原理一致

##### 评分分析

总分 **14 / 15**（第 1 问 5 分 + 第 2 问 8 分 + 第 3 问 2 分）

**第 1 问（算法思想）：5 / 5 分**

| 方面 | 得分 | 说明 |
|------|:----:|------|
| 核心思路 | ✅ 满分 | 三指针线性扫描，每次移动最小元素指针，正确 |
| 表述清晰 | ✅ 满分 | 逻辑完整，易于理解 |

**第 2 问（代码实现）：7 / 8 分**

| 方面 | 得分 | 说明 |
|------|:----:|------|
| 功能正确 | ✅ 满分 | 三指针遍历，能正确找出公共元素 |
| 边界处理 | ✅ 满分 | `while (i < n && j < n && k < n)` 任一越界即终止 |
| 代码简洁性 | ❌ 扣 1 分 | `else if` 链通过 `<=` 逐条判断最小值，逻辑正确但较冗长。参考答案用 `maxNum` + 统一条件判断更简洁，且一次可推进多指针（效率略高） |

**第 3 问（复杂度分析）：2 / 2 分**

| 方面 | 得分 | 说明 |
|------|:----:|------|
| 时间复杂度 | ✅ 满分 | 每个指针最多移动 $n$ 次，$O(n)$ |
| 空间复杂度 | ✅ 满分 | 仅常数个变量，$O(1)$ |

##### 修改建议

1. **简化最小值判断**：可将 `else if` 链替换为参考答案的 `maxNum` 方式，只需计算一次最大值，然后统一推进所有小于最大值的指针，代码更紧凑
2. **使用 `n` 为公共长度**：当前取 `n = A.length`，依赖三个序列长度相同（题目已给定），可改用参数 `int n` 传入，意图更明确
