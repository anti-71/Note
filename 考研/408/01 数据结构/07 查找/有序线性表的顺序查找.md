---
名称: 有序线性表的顺序查找
章: 第七章 查找
节: "[[第二节 顺序查找和折半查找]]"
tags:
  - 知识点
index: 2
---
##### 基本思想

若在查找之前已知表是关键字 **有序** 的，则在查找失败时，无须继续比较到表的另一端即可提前返回查找失败的信息，从而降低查找失败的平均查找长度

假设表 L 按关键字从小到大排列，查找顺序为从前往后，待查元素的关键字为 key

当查找到第 i 个元素时，若发现该元素的关键字小于 key，而第 i + 1 个元素的关键字大于 key，则可立即判定查找失败

因为表中第 i 个元素之后的所有元素关键字均大于 key，故表中不存在关键字等于 key 的元素

可以用如下图所示的判定树来描述有序线性表的查找过程

![image.png|444x217](https://shiyuzhe.oss-cn-hangzhou.aliyuncs.com/Tuchuang/20260322204820860.png)

树中的圆形结点表示表中实际存在的元素

矩形结点称为 **失败结点**（若有 n 个结点，则相应的有 n + 1 个失败结点），它描述了那些不在表中的关键字范围的集合

若最终查找到某个矩形结点，则表示查找失败

##### 平均查找长度分析

在有序线性表的顺序查找中，查找成功的平均查找长度和一般线性表的顺序查找相同

而查找失败时，查找过程一定会走到某个失败结点

这些失败结点是虚构的空结点，实际并不存在

因此，到达某一失败结点时所经历的比较次数，等于其父结点（路径上最后一个真实元素结点）所在的层数

在等概率查找失败的情形下，**查找失败的平均查找长度** 为

$$\text{ASL}_{失败}=\sum_{j=1}^{n} q_j(l_j-1)=\frac{1+2+\dots+n+n}{n+1}=\frac{n}{2}+\frac{n}{n+1}$$

其中，$q_j$ 是到达第 j 个失败结点的概率，在等概率假设下为 $1/(n+1)$ 

$l_j$ 是第 j 个失败结点所在的层数

> [!note] 
> 有序线性表的顺序查找与后续介绍的折半查找在思想上有本质区别
> 
> 此外，有序线性表的顺序查找的线性表可以是链式存储结构，而折半查找的线性表只能是顺序存储结构