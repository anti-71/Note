---
名称: 单链表的初始化
章: 第二章 线性表
节: "[[第三节 线性表的链式表示]]"
tags:
  - 知识点
index: "2"
---
带头结点和不带头结点的单链表在初始化时有所不同

带头结点的单链表初始化时，需创建一个头结点，并让头指针 L 指向该头结点，其 next 指针初始化为 NULL

```c
bool InitList(LinkList &L){          //带头结点的单链表的初始化
    L=(LNode*)malloc(sizeof(LNode)); //创建头结点
    L->next=NULL;                    //头结点之后暂时还没有元素结点
    return true;
}
```

不带头结点的单链表初始化时，只需将头指针 L 初始化为 NULL

```c
bool InitList(LinkList &L){          //不带头结点的单链表的初始化
    L=NULL;
    return true;
}
```
