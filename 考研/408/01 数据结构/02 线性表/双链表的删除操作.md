---
名称: 双链表的删除操作
章: 第二章 线性表
节: "[[第三节 线性表的链式表示]]"
tags:
  - 知识点
index: "12"
---
删除双链表中结点 \*p 的后继结点\*q

```c
p->next=q->next;            
q->next->prior=p;           
free(q);                    //释放结点空间
```

建立双链表时，同样可以采用类似单链表的头插法或尾插法，但要 **注意**：每次插入新结点时，要同时正确设置 next 和 prior 两个指针，以位置双向链接的完整性

