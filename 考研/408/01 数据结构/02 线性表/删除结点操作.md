---
名称: 删除结点操作
章: 第二章 线性表
节: "[[第三节 线性表的链式表示]]"
tags:
  - 知识点
index: "7"
---
删除单链表的第 i 个数据结点

先检查 i 的合法性，然后找到第 i-1 个结点（前驱），再删除其后继结点，并释放内存

假设找到的第 i-1 个结点为\*p，其后继为被删结点\*q，先将\*p 的 next 指向\*q 的后继结点，然后释放\*q 的存储空间

```c
bool ListDelete(LinkList &L,int i,ElemType &e){
    LNode *p=L;               //指针p指向当前扫描到的结点
    int j=0;                  //记录当前结点的位序，头结点是第0个结点
    while(p->next!=NULL&&j<i-1){ //循环找到第i-1个结点
        p=p->next;
        j++;
    }
    if(p->next==NULL||j>i-1)  //i值不合法
        return false;
    LNode *q=p->next;         //令q指向被删除结点
    e=q->data;                //用e返回元素的值
    p->next=q->next;          //将*q结点从链中“断开”
    free(q);                  //释放结点的存储空间
    return true;
}
```

类似于插入操作，该操作的主要时间也耗在查找操作上，时间复杂度为 O(n)

当链表不带头结点时，需判断被删结点是否为首结点，若是，则要做特殊处理，将头指针 L 指向新的首结点

当链表带头结点时，删除首结点和删除其他结点的操作是相同的。

##### 扩展：删除结点\*p

常规方法需要从头遍历找到\*p 的前驱，时间复杂度为 O(n)

若允许修改结点内容，则可采用一种 O(1)的技巧：将\*p 的后继结点的数据复制到\*p 中，然后删除其后继结点

该方法的局限性在于不能用于删除尾结点（因其无后继）

主要代码片段如下：

```c
q=p->next;                  //令q指向*p的后继结点
p->data=p->next->data;      //用后继结点的数据域覆盖
p->next=q->next;            //将*q结点从链中“断开”
free(q);                    //释放后继结点的存储空间
```
