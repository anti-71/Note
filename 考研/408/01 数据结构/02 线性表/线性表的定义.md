---
名称: 线性表的定义
章: 第二章 线性表
节: "[[第一节 线性表的定义和基本操作]]"
tags:
  - 知识点
index: "1"
---
**线性表** 是由 n（n $\geqslant$ 0）个 **相同数据类型** 的元素组成的 **有限序列**

其中 n 为 **表长**；当 n = 0 时线性表为 **空表**

若用 L 命名线性表，则其一般表示为
$$
 L =(a_1, a_2,\dots, a_i, a_{i+1},\dots, a_n)
$$
式中，a $_1$ 是唯一的“第一个”数据元素，也称 **表头元素**；a $_n$ 是唯一的“最后一个”数据元素，也称 **表尾元素**

除第一个元素外，每个元素有且仅有一个 **直接前驱**

除最后一个元素外，每个元素有且仅有一个 **直接后继**（“直接前驱”和“前驱”、“直接后继”和“后继”通常被视为同义词）

以上就是线性表的逻辑特性，这种 **一对一的线性逻辑关系** 正是线性表名字的由来

##### 线性表的特点

- 表中元素的个数有限
- 表中元素具有逻辑上的顺序性，表中元素有其先后次序
- 表中元素都是数据元素，每个元素在线性表中被视为一个不可分割的单位
- 表中元素的数据类型都相同，这意味着每个元素占有相同大小的存储空间
- 表中元素具有抽象性，即仅讨论元素间的逻辑关系，而不考虑元素究竟表示什么内容
