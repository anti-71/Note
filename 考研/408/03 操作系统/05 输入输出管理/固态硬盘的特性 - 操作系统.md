---
名称: 固态硬盘的特性 - 操作系统
章: 第五章 输入/输出管理
节: "[[第三节 磁盘和固态硬盘]]"
tags:
  - 知识点
index: 14
---
##### 基本概述

**固态硬盘**（SSD）是一种 **基于闪存技术** 的存储设备

其存储介质与 U 盘类似，但容量更大、存取性能更优

一个 SSD 由一个或多个闪存芯片以及闪存翻译层组成，如下图所示

![|459x193](https://shiyuzhe.oss-cn-hangzhou.aliyuncs.com/Tuchuang/20260528185828605.png)

##### 核心组件作用

- **闪存芯片** 替代了传统磁盘中的机械驱动器
- **闪存翻译层** 负责将 CPU 发出的逻辑块读 / 写请求转换为对底层物理闪存的读 / 写控制信号，因此，闪存翻译层相当于代替了磁盘控制器的角色

##### 闪存芯片存储结构

一个闪存芯片由 B 块组成，每块包含 P 页

通常，页的大小为 512 B ～ 4 KB，每块包含 32 ～ 128 页，块的大小为 16 KB ～ 512 KB

##### 读写与擦除规则

- 读 / 写操作以 **页** 为单位进行
- 擦除操作以 **块** 为单位进行，只有在整块被擦除后，才能向其中的页写入新数据

一旦某块被擦除，其所有页均可重新写入一次

每个块的擦写次数有限，经过若干次重复写入后，该块会因磨损而失效

##### 随机写入速度慢的原因

1. 擦除操作耗时较长，通常比页访问慢一个数量级
2. 若需修改一个已包含有效数据的页 $P_i$，必须先将该块中所有有效页复制到一个新的（已擦除的）块中，再执行对 $P_i$ 的写入

##### SSD 相较于传统机械磁盘的优势

由半导体器件构成，**无机械运动部件**，因此随机访问延迟极低，且无噪声、无振动、功耗更低、抗震性强、安全性更高