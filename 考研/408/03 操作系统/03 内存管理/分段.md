---
名称: 分段
章: 第三章 内存管理
节: "[[第一节 内存管理概念]]"
tags:
  - 知识点
index: 16
---
##### 分段基本概念

分段系统将用户进程的 **逻辑地址空间** 划分为若干大小不等的 **段**

> [!example] 
> 一个进程可包含主程序段、子程序段、栈段和数据段，共划分为 5 个段

每个段内部从 0 开始编址，并分配一段连续的地址空间（**段内连续，段间不要求连续**）

##### 分段地址空间特点

由于地址空间被组织为多个具有明确语义的段，整个进程的逻辑地址结构呈现出 **二维特性**：

由 **段号** 和 **段内偏移** 共同确定一个地址

##### 分段逻辑地址结构

分段存储管理的逻辑地址由 **段号 S** 和 **段内偏移量 W** 两部分组成

在下图中，段号占 16 位，段内偏移量也占 16 位，则一个进程最多可拥有 $2^{16}=65536$ 个段，最大段长为 64 KB

![image.png|493x68](https://shiyuzhe.oss-cn-hangzhou.aliyuncs.com/Tuchuang/20260520191717625.png)

##### 页式与分段地址区别

- 在页式系统中，逻辑地址中的页号与页内偏移量对用户透明
- 而在分段系统中，**段号和段内偏移量必须由用户显式提供** —— 这一工作在高级语言中由编译程序自动完成